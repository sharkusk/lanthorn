---
title: The verb column offers words no player can type — a dictionary entry containing one of the story's own input separators is unreachable by construction
status: done
type: bug
priority: low
created: 2026-08-30T14:56:54Z
completed: 2026-08-30T20:56:10Z
commits:
  - 8f511aa0e9abe8bdce82185274dd95f9dd091ccc
context: |-
  branch: main
  head: 0b0e3b5a
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1113

  Spotted by the user in Arthur's verb panel, which lists both `be` and `be?`; decoding the dictionary showed the `?` is genuinely in the entry rather than a ZSCII fallback, and that `?` is one of Arthur's six input separators, so the tokeniser splits `be?` into two words before the parser ever looks it up.
---

--- note 2026-08-30T14:57:18Z ---

## The evidence, measured off `stories/arthur-r74-s890714.z6`

Arthur's verb column lists **`be`** and **`be?`**. The `?` is not lanthorn's rendering — `zscii_to_char` maps unprintable ZSCII to `'?'`, which was the obvious suspect and is the wrong one. Decoding the dictionary directly:

```
dict @0x4a53   entry_len 10   entries 1059

'be'   @0x4cf1  z-bytes 1d4514a594a5   data 44c70021
'be?'  @0x4cfb  z-bytes 1d4554a594a5   data 44f70001
```

Word 1 is `0x1d45` in both — Z-chars 7, 10, 5 → `b`, `e`, shift-A2. They differ only in word 2: `0x14a5` versus `0x54a5`, i.e. Z-char 5 (more padding) versus Z-char **21**, which in alphabet A2 is a literal `?`. Both carry flags `$44` and they hold different verb numbers, `$c7` and `$f7` — two genuinely distinct verbs in the game's own data.

Only **2 of 1059** entries contain a `?` at all: the bare `?` and `be?`.

## Why it cannot be typed

The dictionary header's separator list, read from the same file:

```
Arthur's input separators (6):  .  ,  "  '  !  ?
```

`?` is a separator, so the tokeniser splits `be?` into `be` and `?` before the parser looks anything up. **No sequence of keystrokes reaches that entry.** Infocom used this deliberately: putting a separator character inside a dictionary word makes it unreachable from the keyboard, so it can serve as a slot the game's own code references without a player stumbling into it.

Clicking it in the band composes a line the parser will split.

## The fix, and why the existing rule misses it

`VerbTable::without_sigil_verbs` already drops this class of thing — `#record`, `#command`, `$verify` — and its doc argues the right principle: *"A rule, not a list… this is structure, needs no maintained vocabulary, and there is nothing to disagree with."*

It cannot catch this one, for two reasons worth stating so the fix is not bolted onto it carelessly:

1. It tests the **first** character against two fixed sigils. Here the offending character is **last**.
2. Its sigils are a constant. Here the character comes from **this story's own separator table**, which varies per game and is already parsed by `zvm` out of the dictionary header.

So the new rule is the same shape but sourced from the story: **drop any dictionary word containing one of that story's own input separators.** No maintained list, nothing to disagree with, and it generalises to whatever separators another game declares rather than to the six Arthur happens to use.

## Worth checking while implementing

* **How wide is this?** Sweep the corpus for entries containing a separator and report the count per story. Two in Arthur may be typical or may be an outlier, and the answer decides whether this is a tidy-up or a real source of noise.
* **Which surfaces are affected.** The verb column is where it was seen, but the same dictionary feeds the vocabulary offer, the word reveal and completion. An untypeable word is wrong in all of them for the same reason — though note the reveal ANNOTATES rather than proposes, so the argument there is weaker and wants thinking about rather than assuming.
* **The bare `?` entry is a different case.** It is a single separator character and probably a real parser token (`?` as a help command in some games). Do not sweep it up by accident; a word that IS a separator is not the same as a word that CONTAINS one.
* Verification is the parser, as always: whatever is dropped should be a word `zvm-cli` genuinely cannot be made to match.

--- note 2026-08-30T20:56:54Z ---

## Fixed — Lane B, branch `worktree-agent-a65bfd05e7c2f41a2`, commit `8f511aa0`

**Still reproduced after SQ-1153.** Measured before touching anything: Arthur's
verb column held 238 items, `be?` among them, and it was the ONLY item in the
column with a non-alphanumeric character. SQ-1153 changed the flag byte, not the
grammar table the column is built from.

### Where the rule went, and why not `VerbTable`

Not a fifth `VerbTable` combinator — `StoryVocabulary::without_untypeable_words`,
applied once at **`VocabState::get`**, the one vocabulary seam (SQ-1117). The
verb column, the guidance offer, the word reveal and completion all read that
snapshot, so all four are spared it for the same reason and by the same code. It
also kept the change inside this lane's files: `VerbTable` lives in
`render/command_band.rs`, which a parallel lane owned.

(The reveal is a no-op either way, as the quest suspected: its tokens come from
`split_like_parser`, so a word that splits could never have been one of them.
Dropping it there costs nothing and keeps one rule instead of two.)

### The test is the story's own tokeniser, not a separator table

`Engine::split_like_parser` IS `zvm::dictionary::tokenise`, the routine `read`
itself calls, so the separators are the ones this story declares at its
dictionary header. A word is dropped iff the story's tokeniser yields something
other than exactly one token for it. That keeps a word that IS a separator (the
bare `?`), which tokenises to itself — the distinction the quest asked for. An
engine answering `None` (Glulx, Scott) drops nothing: no splitter, no authority.

### Corpus sweep — it is wider than two

Every story in `stories/` with a readable dictionary, full dictionary not just
verbs. Twelve hold at least one untypeable word:

| story | dropped |
|---|---|
| `arthur-r74-s890714.z6` | `be?` `don't` `end.of.` `int.num` `int.tim` `l.g` `no.word` |
| `shogun-r322-s890706.z6` | `be?` `end.of.in` `int.num` `int.tim` `l.g` `no.word` |
| `zork0-r393-s890714.z6` | `end.of.` `int.num` `int.tim` `no.word` |
| `moonmist-r9-s861022.z3` | 19 possessives — `b's` `buf's` `dee's` `doc's` `g's` `hyde'` `ian's` `iris'` `jack'` `li's` `maid'` `rh's` `tam's` `viv's` `worm'` `f.c` `f.n` `int.n` `l.n` |
| `trinity-r12-s860926.z4` | `p.a.` |
| `enchanter-r29-s860820.z3` | one entry that decodes with spaces in it |
| `Facility.z8` `anchor.z8` `nameless.z8` `photopia.z5` `advent.z6` | `comma,` |
| `LostPig.z8` | `.another` `.other` `blah,` `comma,` `error.` |

Only Arthur and Shogun had one in the VERB column (`be?`) — the rest are nouns
and buzzwords, which is why only Arthur's was ever seen. Nothing legitimate is in
the list.

**Moonmist is the specimen that proves the set has to come from the story.** It
declares `'` a separator, so `dee's` tokenises to `dee` `'` `s`; Enchanter r29,
same vintage, same Version 3, does NOT, and `dee's` is one word there. Verified
both directions. No fixed table gets both right.

### Tests

* `command_band::the_verb_column_drops_a_word_the_storys_own_tokeniser_would_split`
  — Arthur r74/890714 and Shogun r322/890706. Asserts the raw dictionary still
  holds `be?` and that the story splits it, THEN that the column does not offer
  it, that `be` survives, and that ordinary verbs are untouched.
* `vocabulary_offer::moonmist_declares_the_apostrophe_a_separator_so_its_possessives_never_reach_a_player`
  — raw snapshot has them, the seam does not; the bare `'` survives; ordinary
  words unaffected.
* `vocabulary_offer::enchanter_does_not_declare_the_apostrophe_and_keeps_the_same_spellings`
  — the control.
* `vocabulary_offer::a_story_that_lends_no_tokeniser_keeps_every_word` — CI-safe
  (no `stories/` needed), pins the safety property.

Falsified: lifting the call out of `VocabState::get` fails the Arthur/Shogun case
and the Moonmist case with the reported symptom.

Docs: `docs/features/interface.md`, beside the sigil rule it is a sibling of.

Gate: `cargo nextest run --workspace` → 0; 6729 run, 6729 passed, 12 skipped.
`cargo clippy -p app --all-targets -- -D warnings` clean.
