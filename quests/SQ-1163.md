---
title: read_zip_entry_caps_a_huge_inflated_entry keys its scratch directory on the bare process id — the SQ-1131 shape, one caller from biting
status: done
type: bug
priority: low
created: 2026-08-31T06:10:27Z
completed: 2026-08-31T14:58:37Z
commits:
  - f47a25eccd50d619a611bd758ca9b4443f7c78b8
context: |-
  branch: main
  head: d598be8a
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1113

  Spotted by the SQ-1162 lane while fixing the Windows CI crash; it is not failing today because only one test uses that path, which is precisely the state SQ-1131's eight consecutive red CI runs started from.
---

--- note 2026-08-31T06:10:42Z ---

## The site

`hints::tests::read_zip_entry_caps_a_huge_inflated_entry` builds its scratch
directory as `bm-zipcap-{pid}` from a bare `std::process::id()`.

## Why it is not failing, and why that is the worrying part

`std::process::id()` alone is unique per PROCESS. Under nextest that is the same as
unique per test, so it always passes locally. Under `cargo test` — which is what CI
runs — it is unique per BINARY, so every caller of that path in the same binary gets
the SAME directory. Today there is exactly one caller, so nothing collides.

**That is the state SQ-1131 started from.** It cost eight consecutive red CI runs
against a local gate that printed 0 every time: `fs::write` truncates a neighbour's
fixture, and a case's closing `remove_dir_all` deletes what another is halfway
through reading — a correct fixture failing its own assertion, somewhere else,
intermittently. The second caller is what turns this from dormant to that, and the
person who adds it will have no reason to look at the first one's path.

## The fix

`app::scratch_dir("a-tag")`, which is unique per CALL by construction. `hints` is
inside `app`, so it is directly available — the `AtomicUsize`-beside-the-pid form is
only for `zvm`/`gvm`/`scott`, which take no dependencies.

## Why the existing guard did not catch it

`scratch_path_discipline` fails the bare `process::id()` form **in a helper**, and
fails two sites that name the same path. This is neither: it is the bare form
inlined in a single test with no second site to collide with. Worth deciding whether
that guard should also fail an inline bare-pid path — the fix is one call either
way, but the guard is what stops the next one, and CLAUDE.md's own corollary is that
a guard beats a convention.

## Verification

Per CLAUDE.md, a change touching a test that writes to disk wants
`cargo test --workspace` as well as the gate, and **wants it twice** — a race that
has been fixed passes once by luck as well.

Related: [[SQ-1131]] is the original and holds the eight-red-runs measurement;
[[SQ-1162]] is the lane that spotted this while fixing the Windows crash.

--- note 2026-08-31T14:14:12Z ---

SCOPE CORRECTED, 2026-08-31 — measured after the user asked why this had not been actioned. It is much larger than "one test", and the title understates it.

**`scratch_path_discipline` only reads `crates/app/tests/suites/`.** Everything else
is unguarded, and the `src/` unit-test surface is where most scratch directories
actually live.

    process::id() occurrences outside tests/suites/ : 182   (needs triage — the
                                                             call has other uses)
    in crates/app/src/hints.rs alone                : 10

The `hints.rs` ten, for shape:

    1635 bm-hintidx-{pid}        2032 bm-zipcap-{pid}
    1647 bm-lsb-{pid}            2157 bm-route-{pid}
    1736 bm-zblorb-{pid}         2183 bm-gblorb-{pid}
    1754 bm-{tag}-{pid}   HELPER 2222 bm-resolve-{pid}
    2253 bm-{tag}-{pid}   HELPER 2238 bm-resolve-nosibling-{pid}

**The two HELPERS are the dangerous ones** and are exactly the form SQ-1131 named:
parameterised by `tag`, so two callers who happen to pass the same tag get the same
directory. `scratch_path_discipline` was written to fail precisely that shape — and
cannot see it here, because it does not look in `src/`.

`crate::scratch_dir(tag)` (`lib.rs:155`) is the cure and is already available inside
`app`: it appends a per-CALL `AtomicUsize`, so two callers with one tag still get two
directories. Note it also does `remove_dir_all` + `create_dir_all` itself, so each
call site's own setup/cleanup must be reconciled rather than left doubled.

## What the fix has to include

1. **Triage the 182** — `process::id()` has uses that are not scratch paths. Report
   the real count.
2. **Convert the scratch-path ones in `app`** to `crate::scratch_dir`. In
   `zvm`/`gvm`/`scott` (zero external dependencies) spell an `AtomicUsize` beside
   the pid, per CLAUDE.md.
3. **Widen the guard so `src/` is covered**, which is the durable half — otherwise
   the next unit test written in a source file reintroduces the whole class. That is
   this project's own corollary: a guard beats a convention, and this guard has a
   blind spot the size of the source tree.
4. Per CLAUDE.md, a change touching tests that write to disk wants
   `cargo test --workspace` as well as the gate, **twice** — a fixed race passes once
   by luck too.

## Why it sat unactioned

I filed it and then asked twice whether to dispatch it, against the standing rule
that a spin-off is fixed in the same effort or dispatched immediately. The user
caught it. Recorded because the rule exists to stop exactly this accumulation.

--- note 2026-08-31T14:59:01Z ---

DONE, 2026-08-31 — lane branch `worktree-agent-accc0bd759e31bd6a`, commit f47a25ec.

## The scope note's premise was wrong, and the real blind spot is a different one

**`scratch_path_discipline` was never limited to `crates/app/tests/suites/`.**
`rust_sources()` has always walked every `.rs` file under `crates/`, and
`in_test_code()` has always counted a `#[cfg(test)]` module in a source file. Measured
by instrumenting the scan: **215 sites, 139 of them under a `src/` directory**,
`crates/app/src/config.rs` alone contributing 36 — more than every file under
`tests/suites/` put together. The `src/` unit-test surface was already guarded.

The 182 figure in the previous note counted raw `process::id()` occurrences, not
scratch paths, and the "only reads tests/suites" claim does not survive reading
`rust_sources`.

## What the real hole was — and the file admitted it

The first case failed only the **bare** pid, and its own doc said a helper
interpolating a `tag` satisfied it and was left "a hand-maintained invariant".
Triage of the 215:

    bare pid, in a helper      0   <- case 1 already holds
    bare pid, inside #[test] 152   <- safe: a case body runs once per process,
                                      and case 2 stops two naming one directory
    extra holes, inside test   9   <- same, safe
    extra holes, IN A HELPER  55   <- THE uncovered class
      of which already counted  4  (fetch_worker, story_info, border_corpus,
                                    zvm/tests/save_interop)
      needing conversion       51

The two `hints.rs` helpers the note flagged are in that 51, and are exactly
SQ-1131's exemplar with a tag put back. `zvm-cli/tests/disk_image.rs` was worse: two
separate helpers, `Adf::write` and `write_temp`, both resolving to
`zvm-cli-<pid>-<name>` — one shared name, two sites, invisible to case 2 because
the `{name}` hole meant the skeletons were not compared.

## The fix

1. **51 helpers converted by hand** (paren-balanced review, no regex sweep). Inside
   `app`: `crate::scratch_dir(tag)` / `app::scratch_dir(tag)`. Below `app` —
   `cli-host`, `zvm-cli`, `scott-cli` cannot depend on it — an `AtomicUsize` beside
   the pid. Each site's own `remove_dir_all` + `create_dir_all` reconciled against
   the one `scratch_dir` already does rather than left doubled. `hints.rs`'s eight
   inline sites went too, since the quest was filed on one of them.
2. **The guard now asks for the counter, not for a distinguishing name.**
   `a_shared_helper_names_more_than_the_pid` becomes
   `a_shared_helper_counts_its_calls`: a scratch path built outside a `#[test]` must
   bump an `AtomicUsize` in the SAME function. A counter three frames up is a fact
   the reader of the helper cannot see, which is the arrangement being replaced.
3. **`the_scan_reaches_unit_tests_in_src`** — a new third case. The belief this
   quest was filed on was reasonable *because nothing said otherwise*, and nothing
   would have failed had a later edit narrowed the walk: the vacuity floor is met by
   `tests/` alone. The claim is now an assertion.
4. `CLAUDE.md`'s SQ-1131 paragraph updated — it still said the guard "cannot see a
   tag two callers happen to spell the same way".

## Falsification

Restored `hints::tests::scratch`'s `bm-{tag}-{pid}` and the case failed, naming
`crates/app/src/hints.rs:1749`. That failure is simultaneously the guard working and
the proof the scan reaches `src/`.

## Verification

`cargo check --all-targets` clean; `cargo clippy -p app -p cli-host -p zvm-cli
-p scott-cli --all-targets -- -D warnings` clean; `cargo nextest run` over those four
crates = 4754 passed, 0 failed; and **`cargo test --workspace` twice**, per the disk
rule — 0 failures both times, because a fixed race passes once by luck as well.

## Left alone

`crates/app/tests/suites/gallery_manifest.rs:353` creates a directory
`app::scratch_dir` has already created — pre-existing, harmless, not this lane's.
The 152 bare-pid sites inside `#[test]` bodies are correct as they stand.
