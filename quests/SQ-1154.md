---
title: --colour terminal is snapped to the Standard table then read back through the machine's, so original media reports a colour that is not the terminal's
status: done
type: bug
priority: low
created: 2026-08-30T16:58:12Z
completed: 2026-08-30T20:28:06Z
commits:
  - 697f3147fcb7faf984252e00cd346abacfa53003
  - 5f09ba868ce9ccc30815421adf05e81142a4891e
context: |-
  branch: main
  head: 5c51998f
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1113

  Reported by the user while confirming that Omarchy live theme switching does reach lanthorn through palette:N roles: the same launch honours --colour terminal on a bare story file and not on a release floppy.
---

--- note 2026-08-30T16:58:35Z ---

## The mechanism, traced but NOT yet reproduced

Read off the code, not measured on a running game. **Reproduce before fixing** — the reasoning below is a hypothesis with a clear shape, and this repo's own history says a self-consistent chain of reasoning is exactly what produces a confident wrong answer.

`$2C`/`$2D` hold colour NUMBERS, not RGB (§8.3.3), so the host's own default page and ink have to be expressed as a number. `colors::host_default_colour_pair` does that with `nearest_standard_colour`, which searches `STANDARD_COLOUR_RGB15` — the **§8.3.1 Standard** table — for the entry nearest the terminal's probed RGB.

Meanwhile `startup.rs` sets the palette from the MEDIUM. Its own comment says why, and the reason is good:

> the palette is set either way, being a table the artwork's colour numbers resolve through rather than a claim about the default page

So on a release floppy the round trip crosses two tables:

1. terminal RGB → nearest **Standard** number
2. that number → RGB through the **machine's** palette (Amiga, Macintosh, IBM CGA)

The two agree on a bare story file, where the palette is `Standard`, and disagree on original media. `--colour terminal` then reports a colour that is not the terminal's, which is the symptom.

Neither half is wrong alone. The palette MUST be the machine's or the artwork's colour numbers resolve wrongly, and that is settled. The gap is that the RGB→number step does not know which table its answer will be read back through.

## The three arms are not equally affected

* `--colour machine` is unaffected by construction — the pair comes from the machine and is read back through the machine's own table.
* `--colour terminal` and `--colour theme` both go through `host_default_colour_pair`, so both should be wrong on original media in the same way. **Check `theme` too**; the report named only `terminal`.

## Likely fix, and the thing to be careful of

Choose the nearest entry in the **palette that is actually in force** rather than always the Standard one — `nearest_standard_colour` becomes nearest-in-a-given-table, and the caller passes the live palette.

Two cautions:

* **A machine palette may not span the space.** IBM CGA at two colours cannot express an arbitrary terminal grey at all, and the honest answer there may be "this launch cannot report the terminal's colours", not "here is the least-bad index". Decide that deliberately rather than letting `min` decide it.
* **The palette is process-global** (`zvm::screen::set_palette`) and this repo has a long scar from that — see the palette-lock discipline in CLAUDE.md. Any test written for this must take the shared lock via `app::v6_palette`, and reading a colour without stating a palette is itself the SQ-0958 defect.

## Verification

The oracle is a real launch, not a unit test: run the same story as a bare file and off its release medium with `--colour terminal`, and compare what `$2C`/`$2D` end up holding and what the pane actually paints. `/dump-windows` and `/dump-cells` are the two instruments. Pin BOTH `honor_game_colours` modes — a colour area with one mode pinned has masked every game-colour regression in this repo's history.

Related: SQ-1082 built the three-source chain; SQ-0956 and SQ-0958 are the palette scars.

--- note 2026-08-30T17:00:15Z ---

## Scope confirmed by the user, and the good news: precedence is already right

> "make sure we apply the same fix for `--colour theme`. this argument should always take precedence over the machine colors, but still allow the game colours to be used (unless that is also disabled)"

Three claims, and **two of them already hold**. Checked rather than assumed:

**1. `theme` needs the same fix — yes.** Both `Theme` and `Terminal` go through `host_default_colour_pair`, so both express their answer as a §8.3.1 Standard number and both get it read back through the machine's table on original media. The reported symptom is `terminal`; `theme` is broken identically and has simply not been noticed. Fix both, test both.

**2. The argument already beats the machine — verified, no change needed.** `colors::host_default_colours` gives `Theme` the themed style and `Terminal` a deliberate `Style::default()` (dropping the theme), and NEITHER arm consults the `machine` parameter at all. Only the `Machine` arm does. There is no fallback to machine colours to remove.

That holds on the restart path too, which is where this repo's scars are: `reset.rs:118` calls the same `colors::host_default_colours`. SQ-1082 collapsed the two copies of this chain into one function precisely so `--colour` could not be added to one and forgotten in the other — the SQ-1022 defect class (`reset.rs` re-deriving a boot fact differently from `startup.rs`) is already closed here.

One shape difference worth knowing, benign: `startup.rs` applies the two-colour card only `if colour_source == Machine`, while `reset.rs` folds the card into the `machine` argument (`card.or_else(|| machine_default_colours())`) and lets `host_default_colours` ignore it for the other two arms. Same net behaviour by two routes; the `reset.rs` shape is the tidier one and is worth preferring if this code is touched.

**3. Game colours stay live unless separately disabled — already true.** `honor_game_colours` is the other axis and short-circuits before any of this: `host_default_colours` returns `None` when it is off, which declares the interpreter colourless under §8.3.2 and leaves the VM's own black-on-white seed alone. `--colour` is inert while that is off, which is the documented intent and matches the requirement exactly.

## So the fix is narrower than the title suggests

Nothing about precedence or about game colours needs to change. The whole defect is the **table** the RGB→number step searches: it must be the palette actually in force, not always Standard. One function (`nearest_standard_colour`), its two callers inside `host_default_colour_pair`, and a decision about what to do when the live palette cannot express the colour at all.

--- note 2026-08-30T17:03:45Z ---

## Correction: my proposed fix does NOT deliver the stated requirement (2026-08-30)

The user pushed back on the framing and was right. Recording it, because the note above would otherwise send the next reader down the wrong road.

> "essentially, when we specify theme or terminal as the colours, we should completely ignore the machine colours (similar to how a raw z-machine file is handled). Is that the fallout of your proposal?"

**No, it is not.** My proposal — search the palette in force instead of the Standard table — keeps the machine's palette and picks the index that happens to render nearest the host's colour through it. That is COMPENSATION, not exclusion. Two problems with it:

* It cannot succeed on a narrow palette. IBM CGA showing two colours has no entry near an arbitrary terminal grey, so the answer degrades exactly where it would be most visible.
* It still leaves the machine's table deciding what the host's own default LOOKS like, which is the thing the requirement says to stop doing.

## The mechanism, now confirmed rather than hypothesised

Traced through the constructor: the resolved pair reaches `session.rs:956` as
`machine.set_default_colours(bg, fg)` — **colour NUMBERS** — and the renderer resolves numbers to RGB through the active palette. So the pane's default page and ink are painted by looking the host's answer back up in the machine's table.

On a bare story file the palette is `Standard` and the round trip is lossless, which is exactly why `--colour terminal` looks right there and wrong off a floppy.

## What the requirement actually asks for

The colour NUMBER is the wrong currency for the PAINT. There are two consumers of that pair and they have been conflated:

1. **What the story is told** in `$2C`/`$2D` — must be a number (§8.3.3), and a number is all the story can be given.
2. **What lanthorn paints the pane with** — needs no number at all. lanthorn draws that surface itself and can use the host's real RGB.

So under `--colour theme` / `--colour terminal` the pane's default should be the host's colour DIRECTLY, with the machine's palette not consulted for it. The story still gets a number in the header, because that is the only thing the header can hold — and any imprecision there is a claim to the story, not a wrong pixel on screen.

**Game colours are unaffected and must stay unaffected.** A story's own `set_colour` names a colour NUMBER and resolves through the machine's palette; that is a game colour, and the user's condition is explicit that those keep working unless `honor_game_colours` separately disables them. Only the DEFAULT page and ink change hands here.

## Still to do before any of this is built

Reproduce it. Everything above is read off the code. Run one story as a bare file and off its release medium with `--colour terminal` and again with `--colour theme`, and record what `$2C`/`$2D` hold and what the pane actually paints, in both `honor_game_colours` modes. `/dump-windows` and `/dump-cells` are the instruments. The measurement may still surprise us — and the fix is now a renderer change rather than a one-function change, which is a good reason to be sure first.

--- note 2026-08-30T17:06:38Z ---

## The right model, from the user — and it is implementable nearly literally (2026-08-30)

> "when `--colour theme | terminal` we use the raw z-machine path with native media. The opposite is true when we set `--colour machine`, then we use the media path with a raw z-machine file."

`--colour` selects which colour REGIME applies, independent of what the medium is. That is a far better statement of the requirement than either of my framings above, and it makes the fix smaller rather than larger.

**Half of it already works.** `--colour machine` on a bare story file is exactly SQ-0928's `--system-colours`, subsumed by SQ-1082: the opt-in that licenses a machine's §8.3.3 pair when the MEDIUM did not name a machine but `--interpreter` did. So the media-regime-on-a-raw-file direction is implemented and tested. Only the mirror is broken.

## Why the mirror is achievable: the artwork does not use this palette

Checked, and it is the fact the whole design turns on. `zvm::screen::set_palette` says it selects "the palette standard colour numbers resolve through, process-wide" — and the artwork does not go through it. `app::graphics` keeps its OWN `current_palette`, set per picture from the resource itself (`set_current_palette_from(resnum)`, the archive's PLTE). Pictures resolve through the archive's palette; only TEXT colour numbers resolve through `zvm::screen`'s.

So the text palette can be left at `Standard` under `--colour theme|terminal` — the raw-file path, exactly as the user describes — without touching a single pixel of artwork.

That also disposes of my previous note's proposal (separate the paint from the reported number, a renderer change). Unnecessary: with the text palette at Standard the round trip is lossless again, the header carries a number that means what it says, and the pane paints what the host asked for. **Prefer this over the renderer change.**

Note the consequence and accept it deliberately: a game's own `set_colour(4)` would then resolve as Standard red rather than Amiga red. That is the correct reading of the requirement — on the raw path it IS Standard red, and the user's rule is that theme/terminal means the raw path. Game colours keep working; they simply resolve through the table the chosen regime names.

## The one real edge: a two-colour card palette carries TWO facts

`Palette::IbmCga` is not only a table. CLAUDE.md and `startup.rs` both record that it also carries "the fact that the display has one bit", which `zvm::screen::two_colour_card_request` reads — and that answer decides whether the game asks for MONO or COLOUR plates.

So on a two-colour card press, swapping the text palette to `Standard` is not purely a text change: it could alter which artwork the story requests. That coupling has to be broken or explicitly excepted before this lands. Two candidate shapes, both needing thought rather than a coin toss:

* keep the display-depth fact where it is and make the text table separately selectable, so the regime changes the table without changing the declared depth; or
* except the two-colour-card case from the regime switch, and say so in the flag's own documentation, because a silent exception is how `--colour` stops meaning what it says.

## Reproduce first, still

Everything here is read off the code. Before building: one story bare and off its release medium, `--colour terminal` and `--colour theme`, both `honor_game_colours` modes, recording what `$2C`/`$2D` hold and what the pane paints. Add a two-colour-card press (a CGA `.cg1`) to that matrix specifically, since it is the case with the coupling.

--- note 2026-08-30T17:07:06Z ---

## Correction to the note above: do NOT gate this on reproducing the mechanism (user, 2026-08-30)

> "we know it works because raw z-machine files work, this should be no different"

Right, and the previous note's "reproduce first, still" is over-cautious. The raw path is an EXISTENCE PROOF: `--colour terminal` and `--colour theme` already resolve correctly on a bare story file, through the `Standard` text palette. Routing native media through that same path makes it correct by construction — there is no mechanism left to confirm, because the fix does not depend on the diagnosis being right. It depends on the raw path working, which it demonstrably does.

So: implement it. Do not spend a lane re-measuring why the broken road was broken.

**The one thing the raw path cannot vouch for**, and the only part still owed real verification: a bare story file never has a two-colour card, so nothing about `Palette::IbmCga` carrying a display-depth fact (read by `two_colour_card_request`, deciding mono vs colour plates) is exercised by the existence proof. That coupling is the single open question and the only place a measurement is owed — a CGA press with `--colour terminal`, checking which plates the story asks for before and after.

Everything else in this quest is settled: the regime model, why the artwork is unaffected (it resolves through `app::graphics`'s own per-picture palette, never `zvm::screen`'s), and that game `set_colour` resolving as Standard under theme/terminal is the correct reading rather than a regression.

--- note 2026-08-30T17:08:51Z ---

## Solved on paper: it is ONE predicate, and it already exists (2026-08-30)

The user asked how the CGA coupling is handled on the raw path. The answer dissolves the last open question in this quest: **it is not handled, it is never reached** — and the thing that excludes it is the same gate that excludes the machine's colour pair.

`PictSource::two_colour_card_screen` needs three facts together, and the second is the one that matters:

> the LAUNCH may present its machine (`ProfileSource`'s licence, SQ-0928: a medium always, an asked-for machine on request, **a bare story file never**) — which is also what stops a `.cg1` opened beside a plain `.z6` from reaching here

And both machine-colour questions hang off that one predicate:

```rust
pub fn machine_colours_licensed(&self) -> bool {
    self.interpreter_source.licenses_machine_colours(self.system_colours)
}
pub fn machine_default_colours(&self) -> Option<(u8, u8)> {
    self.machine_colours_licensed().then(|| self.interpreter_profile.default_colours()).flatten()
}
// machine_two_colour_colours() is licensed by the same rule, and its own doc says so
```

## The fix

`--colour theme | terminal` should mean **this launch does not present its machine** — exactly the state a bare story file is in. Withhold that one licence and everything follows without a second change:

* `machine_default_colours()` → `None`, so the host's pair is what the story is told;
* `machine_two_colour_colours()` → `None`, so `two_colour_card_screen` returns `None`;
* `Palette::IbmCga` is therefore never installed and the text palette stays `Standard`;
* artwork is untouched — it resolves through `app::graphics`'s own per-picture palette, never `zvm::screen`'s;
* a game's own `set_colour` resolves through `Standard`, which is what it does on the raw path, and is the consistent reading of the regime.

**The "one real edge" recorded two notes above is withdrawn.** The two-colour card is not an exception to the regime; it is governed by the regime, through the same predicate. There is nothing left needing a measurement it cannot get from the existence proof.

## Which makes the symmetry literal

The licence is what makes a medium a medium, for colour purposes. `--colour machine` GRANTS it on a bare story file — already implemented, already tested, that is SQ-0928's `--system-colours` subsumed by SQ-1082. `--colour theme|terminal` should WITHHOLD it on a medium. One predicate, driven from both ends.

## Implementation notes

* Find where `interpreter_source` / `ProfileSource` is decided and let `--colour` override the licence, rather than special-casing `--colour` at each of the machine-colour call sites. The call sites already agree today because they all ask the one predicate; keep it that way.
* `startup.rs`'s `if cfg.colour_source == ColourSource::Machine` guard around the two-colour pair becomes redundant once the licence covers it. Removing it is the tell that the change landed in the right place — if it still has to be there, the licence is not doing the work.
* `reset.rs` needs nothing: it already asks the same predicate through the same helpers, which is what SQ-1082 unified.
* Tests: a medium press with `--colour terminal` must report the host's pair AND leave the palette `Standard`; a CGA press with `--colour terminal` must not install `IbmCga`; and `--colour machine` on a bare `.z6` with `--interpreter` must still do what it does today. Both `honor_game_colours` modes, and take the shared palette lock (`app::v6_palette`) — reading a colour without stating a palette is SQ-0958 verbatim.

--- note 2026-08-30T18:27:58Z ---

## Built, as designed — plus ONE line the design did not name (2026-08-30)

Commit `697f3147` on branch `worktree-agent-a72977dd37d8f9a82`. The predicate change is exactly the previous note's, and the tell held: the `if cfg.colour_source == ColourSource::Machine` guard in `startup.rs` is gone.

### Where the override went

`Config::machine_colours_licensed`, matched on `colour_source` around the existing `interpreter_source.licenses_machine_colours(system_colours)` call. NOT on `ProfileSource` itself and not on `interpreter_source`'s VALUE — forcing the source to `Fallback` under a host regime would also move `advertised_interpreter_number`, `native_font::resolve`'s `source == Medium` disk-font rung, `MachineBoot` and `std_window`, which is geometry and typeface, not colour. `ProfileSource` answers *how the machine was learnt*; the regime is a config-level policy over that answer. It is still ONE predicate and no call site special-cases `--colour`.

### The one extra line, and why it was needed

`startup.rs`'s palette gate asked the licence of `IbmXzip | IbmYzip` only. That was sufficient when falling through to the IBM PC was the ONLY way to be unlicensed — no other table could reach an unlicensed launch. `--colour terminal` is a second way, so an **Amiga** floppy kept `Palette::Amiga` installed and the reported symptom survived verbatim: the host's RGB snapped to §8.3.1 and read straight back through the machine's table. The gate now asks the licence of the whole table.

It moved to `Config::machine_text_palette(zversion)` so the suites measure the shipped rule rather than a copy of it. One behaviour change falls out beyond the quest: `--interpreter 4` on a bare `.z6` **without** `--colour machine` now resolves through §8.3.1 where it used to install the Amiga's table. That launch was already unlicensed for its pair, so this makes it consistent rather than new — nothing in the gate or `cargo test --workspace` moved on it.

### Also withdrawn under a host regime: the PERIOD LOOK

`period::resolve` takes `machine_colours_licensed` as its `licensed` argument (its own doc: "a `$2C`/`$2D` pair and a period look are the same kind of claim about the same machine, and it would be incoherent to license one and not the other"). So `--colour theme|terminal` now also declines the machine's screen dress. Coherent with the regime — the raw path has no period look either — but it was not enumerated in the design note, so it is named here.

### Verification

`crates/app/tests/suites/colour_regime_media.rs`, in the `engines` group, 7 cases, all real presses under the shared palette lock, both `honor_game_colours` modes:

* `Journey - The Quest Begins.adf` under `terminal` and `theme` — palette `Standard`, `$2C`/`$2D` read back as the host's pair, not the Amiga's; the same floppy under `machine` still `Palette::Amiga` and the Amiga's pair.
* Zork Zero DOS 360K Disk 1 (`.CG1`) — under `machine` the card is read and `IbmCga` installed (`two_colour_card()` true, the display-depth fact); under `terminal`/`theme`, in BOTH honour modes, `two_colour_card_screen` is `None`, `IbmCga` is never installed and the depth never declared. That is the one question the raw path could not vouch for, now measured.
* `journey-r83-s890706.z6` + `--interpreter 4` + `--colour machine` → `ProfileSource::Asked`, licensed, Amiga table and pair — unchanged. Without the opt-in, `Standard` and the host's pair.
* The artwork MEASURED, not argued: the first plate off the Amiga floppy decodes byte-identical under `machine` and `terminal`.
* `honor_game_colours = false` still outranks every regime (reports no pair at all, §8.3.2).
* CI-safe half in `config.rs`: `the_host_colour_regimes_withhold_the_machine_however_it_was_named`, since every media case skips vacuously on CI.

**Falsified**: reverting the predicate fails `a_release_floppy_under_a_host_regime_is_told_the_hosts_pair` with `left: Amiga, right: Standard` — the reported round trip — and `a_cga_press_under_a_host_regime_shows_no_card` with the card back.

Gate `0`, `Starting 6715` = `6715 passed`. `cargo test --workspace` clean **twice** (palette-global + a suite that writes to disk). Clippy `--workspace --all-targets -D warnings` clean.

`reset.rs` verified by reading rather than assumed: it asks `two_colour_card_screen(&state.config)` and `machine_default_colours()`, both of which answer `None` under a host regime, and it never re-installs the machine palette at all. No change needed.

Docs: `docs/features/interpreter.md` gains the mirror paragraph beside SQ-0928's; `docs/readme-next.md`'s `--colour` line now says it selects a regime.

--- note 2026-08-30T19:06:34Z ---

REOPENED from `confirm` — the fix is incomplete. User found it on Arthur, and their own third experiment isolated it cleanly.

OBSERVED (user):
- `Arthur - The Quest for Excalibur.adf` (r54/890606) `--colour terminal` → BLACK background
- same floppy `--colour machine` → GRAY background (so `--colour` IS reaching something)
- `arthur-r74-s890714.z6 --interpreter 4 --colour terminal` → ALSO BLACK
- `arthur-r74-s890714.z6` with no `--interpreter` → correct terminal background

The third line is the one that matters: no medium, no archive, no palette table needed. Advertising interpreter 4 is sufficient. (Note the first two files are DIFFERENT RELEASES — r54 vs r74 — so that pair alone was not a controlled comparison; the bare-file run is.)

MECHANISM, read from the source not inferred: `zvm::screen::amiga_global_colour_pair` implements §8.3.1's Amiga rule — one colour pair shared by ALL windows, and changing either recolours every window to match. It is gated by `machine_rule` on (a) version 6, (b) Flags 1 bit 0 colours-available, and (c) header `$1E` naming the machine — all read back OUT OF THE HEADER, deliberately, so the rule survives `@restart`, `@restore` and a host Save State without anyone carrying it.

Nothing in that chain asks `machine_colours_licensed`. So a host regime withholds the machine's DEFAULT PAIR while leaving the Amiga's global-pair BEHAVIOUR live, and Arthur's own `set_colour` is then globalised to every window — the black ground.

WHY IT IS THIS QUEST AND NOT A NEW ONE: the commit's claim is "the fix is one predicate and everything follows". This is a machine-colour question that does not hang off the predicate, which is the same shape as the `startup.rs` palette gate the lane already had to widen (written when falling through to the IBM PC was the only way to be unlicensed). The user's framing has been consistent throughout: `--colour theme|terminal` should leave a launch in the state a raw story file is in, and on the raw path this rule is inactive because no `$1E` names a machine.

THE DESIGN TENSION, unresolved and needing the user: the licence lives in `app`'s `Config`; the rule deliberately reads the HEADER so it survives restore. The lane explicitly REJECTED forcing `interpreter_source` to `Fallback` under a host regime — correctly, because `advertised_interpreter_number` also drives `native_font::resolve`'s disk-font rung, `MachineBoot` and `std_window`, i.e. GEOMETRY and TYPEFACE, which `--colour` has no business moving. So "just stop advertising 4" is not available. The licence has to reach the rule some other way, and it must survive a restore, which is the whole reason the current terms are header-read.

NOT YET CHECKED: whether `global_colour_pens` is the only per-machine rule with this exposure. `machine_rule` is a shared shape and other `MachineProfile` members go through it (SQ-0872) — every one of them should be audited against the licence, not just this one, or the next regime bug is already written.

DISCRIMINATOR worth keeping: `--game-colours off` should clear the black, because `machine_rule` requires Flags 1 bit 0. That is a symptom-level workaround, not the fix.

The committed work (`697f3147`) is otherwise sound and its own tests pass; this is an addition to it, not a revert. Do not merge as "complete" — the quest's claim has to be true before the commit message's "everything follows" is honest.

--- note 2026-08-30T19:08:20Z ---

AUDIT of the per-machine rules, answering "do any other machines behave this way?" — done by reading the table, not inferred.

`machine_rule` has exactly TWO callers in `crates/zvm/src/screen.rs`:

1. `amiga_global_colour_pair` (:1309) → `MachineProfile::global_colour_pens`.
   **Amiga alone.** Pinned by `the_amiga_is_the_only_machine_with_global_colour_pens`,
   asserting `pens == vec![AMIGA_INTERPRETER_NUMBER]` with "ZMSD §8.3 names the
   Amiga and no other". Apple IIe/IIc/IIgs, Macintosh, Atari ST, IBM PC, C64 and
   C128 all set it false. So the reported black ground is Amiga-only and cannot
   appear on another machine by this route.

2. `machine_screen_pair` (:1405) → `MachineProfile::v6_screen_page`.
   **TWO machines: Amiga and Macintosh** (SQ-0846's pair; the Apple rows state a
   page as real as the Mac's and deliberately do not claim it). Also does not ask
   the licence. NOT YET VERIFIED under a host regime.

THE DISTINCTION THAT MATTERS, and it is the general lesson of this reopening:
`machine_screen_pair` hands back `header_pair(mem)` — a VALUE, and the licence
already corrects that value (the lane's own case asserts `$2C`/`$2D` carry the
terminal's pair under a host regime). `global_colour_pens` gates a BEHAVIOUR —
"one pair for all windows, and a change to either recolours the whole screen" —
and correcting a value does not correct a behaviour. That is precisely why
Arthur's own `set_colour` black spread everywhere: nothing we withheld could
stop it, because we only ever withheld numbers.

So the predicate as shipped licenses VALUES and not BEHAVIOURS, and any future
per-machine rule that gates behaviour will have the same hole. Whatever fix
lands should make that explicit at `machine_rule` itself rather than at the one
call site Arthur happened to expose.

STILL TO CHECK: a Macintosh press (or `--interpreter 6`) under `--colour terminal`,
for caller 2. "Less likely to be wrong" is not "verified".

CLEARED, not a concern: `MachineProfile::one_screen_palette` (Amiga-only, consumed
at `startup.rs:1222` via `picts.set_screen_palette`) governs whether a scene's
table repaints the BORDER. That is artwork, which `--colour` deliberately does not
touch and which resolves through `PictSource`'s own per-picture palette from the
archive's `PLTE`. Correctly outside the licence. `period_look` is already licensed
(it takes `machine_colours_licensed` as its `licensed` argument).

--- note 2026-08-30T19:17:58Z ---

MEASURED, settling the two candidate mechanisms. Headless boot of `arthur-r74-s890714.z6` with `--interpreter 4` under both regimes, via the lane's own `launch()` helper (scratch case, since reverted — the worktree is clean).

    snap: #1A1B26 -> colour 2  (Standard RGB 0      = pure black)
          #E4E4DC -> colour 9  (Standard RGB 0x7FFF = pure white)

    Machine : palette=Amiga    header(2C,2D)=(12, 9)  pens=true  screen_pair=(fg 9, bg 12)
    Terminal: palette=Standard header(2C,2D)=( 2, 9)  pens=true  screen_pair=(fg 9, bg  2)

`$2C` is BACKGROUND and `$2D` FOREGROUND, pinned by `screen.rs:3807` ("v{v} default
background = black(2)" / "default foreground = white(9)") — so the tuple reads (bg, fg).

HYPOTHESIS 1 (Arthur issues its own `set_colour`, globalised by the pens) is
REFUTED: `amiga_screen_pair` equals the header pair exactly under both regimes, so
nothing has moved the pens at boot. Arthur is not choosing black.

HYPOTHESIS 2 CONFIRMED. The pens rule makes the header pair BE the screen — the
doc's own words, "on the real machine the two registers are not advice, they are
the screen". The header can hold only colour NUMBERS (that is the standard, not a
defect), so the host's true RGB is snapped on the way in and painted back out as
the snapped value. #1A1B26 snaps to 2, and 2 under Standard is #000000. Both of
the user's observations fall out: 12 under the Amiga table is dark grey $444 (the
gray), 2 under Standard is pure black (the black).

CONSEQUENCE FOR THE DESIGN — it holds, and is COMPLETE for this symptom. With the
licence withheld the pens rule is off, v6 windows stay at `ZColour::Default`, and
the host paints #1A1B26 un-snapped, which is exactly what the bare file already
does. No second fix is needed and the lossy snap into `$2C`/`$2D` can stay, because
with the rule off nothing uses those numbers as the ground.

This also shows the round trip the quest was originally filed for was never fully
closed — it was closed for the VALUE the story reads and left open for the GROUND
the screen is painted with, which only the pens rule reaches. Same defect, second
route.

Measurement caveat: boot frame, turn 0. Sufficient here because the symptom is
immediate, but a case pinned for this should still drive a turn or two (per the
"a frame is a fixture, name the turn count" convention).

--- note 2026-08-30T19:21:42Z ---

MEASURED across machines, answering "do any other machines behave this way". Scratch probe on `arthur-r74-s890714.z6` at `--interpreter` 3/4/5/6, both regimes, since reverted (worktree clean).

    i=3 Macintosh Machine : header(bg,fg)=(9, 2)  pens=false  screen_pair=(fg 2, bg  9)  black on white, correct
    i=3 Macintosh Terminal: header(bg,fg)=(2, 9)  pens=false  screen_pair=(fg 9, bg  2)  PURE BLACK ground
    i=4 Amiga     Machine : header(bg,fg)=(12,9)  pens=true   screen_pair=(fg 9, bg 12)  dark grey, correct
    i=4 Amiga     Terminal: header(bg,fg)=(2, 9)  pens=true   screen_pair=(fg 9, bg  2)  PURE BLACK ground
    i=5 Atari ST  both    : screen_pair=None                                             unaffected
    i=6 IBM PC    both    : screen_pair=None                                             unaffected

THE MACINTOSH HAS THE SAME DEFECT, through `v6_screen_page`/`machine_screen_pair`
rather than `global_colour_pens`. A Mac press under `--colour terminal` grounds at
pure black exactly as the Amiga does. Atari ST and IBM PC claim no screen page, so
their windows stay `ZColour::Default` and the host paints the true terminal RGB.

CORRECTION TO MY EARLIER NOTE ON THIS QUEST. I wrote that `machine_screen_pair`
was "less likely to be wrong" because it hands back a VALUE the licence already
corrects, against `global_colour_pens` gating a BEHAVIOUR. That distinction is
wrong and the measurement refutes it. Using the header pair AS THE GROUND is
itself the behaviour that discards the host's true RGB, whichever rule arrives
there — the licence corrects the pair to the snapped host numbers, and snapping
is the loss. The Amiga's pens are merely the louder of two routes to the same
defect. Anyone reading that earlier paragraph should ignore its conclusion; the
audit's inventory of WHICH rules and machines is still accurate.

CONSEQUENCE: gating at `machine_rule` (the shared shape) rather than at
`amiga_global_colour_pair` (where the symptom showed) is not tidiness — it is the
difference between fixing one machine and fixing two. Patching the call site would
have left the Macintosh broken and undiscovered, because no Mac case asserts a
GROUND; they assert pairs, and the pair is correct.

So: not overcomplicating for the Amiga. Under-handling the Macintosh.

--- note 2026-08-30T19:21:59Z ---

A THIRD AXIS FOR RESTORE TESTS, raised by the user asking whether a host Save State stores colour that could conflict with a different `--colour` at launch. It does, and the answer is worth keeping.

WHAT THE ARCHIVE STORES (`crates/app/src/archive.rs`):
- `DisplayListDto::palette` — raw `PLTE` bytes of the Current Palette at save time.
  That is the PICTURE palette, kept so a replayed display list does not decode in
  the wrong colours. Not the text palette, and not touched by `--colour`.
- `ScreenDto::current_fg` / `current_bg` as `ZColourDto` — `Default`, `Standard(n)`,
  `True(v)`, `True24(v)`. For v6 both are written as `Default` deliberately, because
  the v6 WINDOW TABLE is the single source of truth ("so neither mechanism can
  quietly paper over the other going wrong").

Storing NUMBERS rather than resolved RGB is correct and is the archive's
backend-and-terminal-neutral rule working as designed. But a colour number means
nothing without a palette, and after this quest `--colour` CHOOSES the palette.

THE CONFLICT: save under `--colour machine` on an Amiga, restore under
`--colour terminal`. Every `Standard(n)` in the restored window table now resolves
through §8.3.1 instead of the Amiga's table — same numbers, different colours. The
screen will look self-consistent and be wrong, which is the family this whole quest
belongs to.

This is SQ-0958's rule ("no test may assume a palette it did not write") arriving at
RUNTIME rather than in a suite. The restore-test convention in CLAUDE.md currently
names two axes to cover — a different terminal size and a different graphics backend
— and this adds a third: A DIFFERENT COLOUR REGIME. Both directions, since machine→
host and host→machine are not symmetric (one loses the machine's table, the other
acquires one the save never saw).

NOT YET MEASURED, and deliberately not asserted here: whether a cross-regime restore
is actually wrong on screen today, or merely capable of being. The reasoning above is
from reading `archive.rs`, and per this quest's own history a mechanism that reads
obviously is worth measuring before it is believed. Whoever takes the fix should
drive it — restore, then MAKE A MOVE, then assert (restore bugs surface one action
after the restore, when the game next repaints).

--- note 2026-08-30T19:57:50Z ---

## Lane A, the reopening fixed — the licence is now a term of the RULE (2026-08-30)

Commit `5f09ba86` on branch `worktree-agent-a53069e359077bb1e`, `Confirm: SQ-1154`.
Built to the settled design, no redesign.

### The change

`zvm::screen::machine_rule` takes a FOURTH TERM,
`Machine::machine_colours_licensed`, at the shared shape rather than at
`amiga_global_colour_pair`. The three public entry points
(`amiga_global_colour_pair`, `amiga_screen_pair`, `machine_screen_pair`) now take
`&Machine` instead of `&Memory`, so the licence cannot be supplied wrongly by a
caller — it is inherent in the value, per the refactoring policy's "a guard beats
a convention".

`MachineBoot` gains `machine_colours_licensed`, a FIELD (not
`default_colours.is_none()`), and `MachineBoot::resolve` takes it as a REQUIRED
parameter. That enumerated 56 call sites: `startup.rs`, three in `reset.rs`, 35
test suites and 4 examples. The two production sites pass
`cfg.machine_colours_licensed()`. Converted by a paren-balanced parser that
skipped anything it did not recognise (one skip was a string literal inside
`v6_cga_stencil_page`'s source-level guard — a regex would have mangled it).

### One correction to the design note, and it is measured

The brief said "seed it into zvm SCREEN STATE at boot". It must NOT go on
`ScreenState`, and the reason is a hole the design did not name:

* `Machine::restart` (the `@restart` opcode) does `self.screen = screen` from a
  fresh `boot_state_and_screen`, and
* `session::restore_screen` does `machine.screen = screen` from
  `archive::ScreenDto::to_screen`, which ends in `..Default::default()`.

Either would have silently reset the licence — the first back to zvm's default on
an in-VM restart, the second on every host Save State restore. It went on
`Machine`, beside `v6_wrap_regime`, where all three survivals (`@restart`,
`@restore`, host Save State) are free and the design note's "takes it from the
current Config" is what actually happens. zvm's own default is `true`, which is
ZMSD as written (§8.3's escape hatch is "avoid using the Amiga interpreter
number", so on a bare embedding advertising it IS the opt-in).

### The cross-regime restore hazard: MEASURED, NOT REAL

Driven both directions on `arthur-r74-s890714.z6` `--interpreter 4`, restoring
then MAKING A MOVE before asserting:

    SAVE  under Machine : palette=Amiga    header=(12,9) ground=Standard(12)/Standard(9)
    FRESH under Terminal: palette=Standard header=( 2,9) ground=Default/Default
    AFTER Machine->Terminal: header=(2,9) ground=Default/Default   licensed=false
    SAVE  under Terminal: palette=Standard header=( 2,9) ground=Default/Default
    FRESH under Machine : palette=Amiga    header=(12,9) ground=Standard(12)/Standard(9)
    AFTER Terminal->Machine: header=(12,9) ground=Standard(12)/Standard(9) licensed=true

The restoring run's regime decides the default page and ink outright and nothing
from the saving run survives. Nothing to fix. Also measured: Arthur's v6 window
table is EMPTY of explicit colours after the intro (no window carries a non-
`Default` pair), so the `ZColourDto::Standard(n)` half of the concern is not even
exercised by this specimen — and where it is, a stored number resolving through
the showing run's table is the regime read consistently, which this quest already
settled for a fresh launch. Pinned as
`a_host_save_state_does_not_carry_a_colour_regime_across`; recorded in the commit
message too, so nobody re-derives it from `archive.rs`.

### Verification

Four new cases in `colour_regime_media` (the `engines` group), plus a row on
zvm's `the_amiga_screen_pair_is_the_headers_own_default_colours`. They assert the
GROUND (`ScreenModel.bg`/`fg`, `0` = `ZColour::Default` = the host paints its own
RGB) rather than the pair, on BOTH machines, BOTH host regimes, BOTH
`honor_game_colours` modes, with a non-vacuity guard that `--colour machine`
still claims the page. Turn count named: twelve intro taps, `v6_arthur_status`'s
own route to Arthur's first playable frame.

FALSIFIED: reverting the fourth term fails all three ground cases with
`left: (16777218, 16777225)` — `Standard(2)` over `Standard(9)`, §8.3.1 pure
black under white, the reported symptom — while ALL SEVEN pair-asserting cases in
the same file stay green. That asymmetry is the whole reopening, reproduced.

Gate `0`, `Starting 6719` = `6719 passed` (6715 + 4). Clippy
`--workspace --all-targets -D warnings` clean. `cargo test -p app --test engines`
— one process, many threads, the shape nextest structurally cannot see — 313
passed.

Docs: `docs/features/interpreter.md` gains a paragraph on rules-versus-values and
on the restore finding.

### Scope note

`crates/zvm/src/cpu/exec.rs` was outside the lane's stated file scope and was
edited anyway (field + default + one call site): the licence has to live on
`Machine`, which is defined there. No parallel lane owns that file.

### Still open for the user's eye

Only a real press can settle a ground colour: launch
`Arthur - The Quest for Excalibur.adf` (and the bare `arthur-r74-s890714.z6`
with `--interpreter 3` and `4`) under `--colour terminal` and confirm the
background is the terminal's, not black.
