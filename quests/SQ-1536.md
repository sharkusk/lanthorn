---
title: Hotkey/leader-dialog dispatch also gates a command against its own spec.context instead of the live context (same shape as SQ-1535, different surface)
status: open
type: bug
priority: low
created: 2026-09-19T07:00:00Z
commits: []
context: |-
  branch: main
  head: aa96cfc3
  cwd: /Volumes/Videos/Source/babelmap

  Found while fixing SQ-1535 (in-game command palette showing browser-only commands): input.rs's hotkey-dialog leader-letter dispatch (~line 1480) uses the same find_command(name).map(|c| c.context) self-gating pattern the palette used to. Left untouched per that quest's explicit scope (hotkeys.rs is hand-authored from user config, not the reported bug), but the underlying pattern is identical and could misfire if a user ever configures a browser-only command into their [hotkeys] leader dialog.
---
