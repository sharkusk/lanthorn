---
title: Shogun's Macintosh HFS press is unpinned in the release table, and nothing guards the table against a medium it has never heard of
status: done
type: bug
priority: low
created: 2026-08-30T21:00:26Z
completed: 2026-08-30T21:52:42Z
commits:
  - 9b5bb5b93158d8f07d29e40f8411b82e3b51ada8
context: |-
  branch: main
  head: bbf4220b
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1113

  The SQ-1152 lane consulted real_media_releases.rs, found no Macintosh Shogun, and concluded from that absence that Shogun.toast was an unusable Toast CD image — the user corrected it; the file is a bare HFS volume (BD signature at 0x400, volume name "Shogun") that DiskImage::detect handles by content, so the extension never mattered.
---

--- note 2026-08-30T21:00:49Z ---

EVIDENCE, measured on the file itself:

    xxd -s 1024 -l 16 stories/Shogun.toast
    00000400: 4244 c43f 6b62 c836 c598 0100 0007 0003  BD...
    00000420: .... 0653 686f 6775 6e                   volume name "Shogun"

`BD` at offset 1024 is the HFS Master Directory Block signature. That is exactly
`DiskImage::Hfs` — "a Macintosh HFS volume, bare or inside a DiskCopy 4.2 wrapper" —
and `blorb::medium::DiskImage::detect` works on CONTENT, so the `.toast` extension
was never relevant. The user confirms it plays.

THE GAP. `crates/app/tests/suites/real_media_releases.rs` pins four Shogun media —
Amiga `.adf` r295/890321, bare `shogun-r322-s890706.z6`, `shogun_s1.dsk` ProDOS
r311/890510, `Shogun.po` ProDOS r311/890510 — and NOT the Macintosh press. No
comment explains the omission, so it reads as deliberate when it is not.

CLAUDE.md states the invariant outright: "The release every medium in `stories/`
carries is pinned in `crates/app/tests/suites/real_media_releases.rs` and tabulated
in `docs/features/interpreter.md`". That sentence is currently false.

WHY IT MATTERS MORE THAN ONE MISSING ROW — this is the "a guard beats a convention"
case, and it has already cost something. The SQ-1152 lane consulted the table,
found no Macintosh Shogun, and reasoned from that ABSENCE to "Shogun has no
Macintosh press in the corpus". It then justified a capture decision on that
premise and reported it as fact, and I relayed it to the user as fact. An absence
in a hand-maintained table is indistinguishable from a fact about the world, which
is the same failure mode as [[verify-dont-infer]]: a fact the system states outright
(the file's own MDB signature) was recovered instead by inference over a proxy (a
test table's contents).

## What solving it needs

1. **Pin the row.** Mount `Shogun.toast`, read its release and serial from the
   header the way the other rows were established, and add it. Do NOT copy a
   release number from a sibling medium — every Shogun press in the table so far
   carries a DIFFERENT release (295, 322, 311), which is exactly the point of
   CLAUDE.md's "a disk image is a different release, not the same story on other
   media". Tabulate it in `docs/features/interpreter.md` alongside.

2. **Add the guard**, which is the durable half. A source-level case that walks
   `stories/`, asks `DiskImage::detect` of each file, and fails on any detected
   medium the table does not name. It must SKIP VACUOUSLY when `stories/` is absent
   (gitignored commercial media, CI has none) while still failing loudly when the
   directory exists and holds an unpinned press — the pattern is
   `any_v6_story_present()` in `zmsd_screen_compliance.rs`.

3. **Check for other unpinned media** while there. Shogun is the one that surfaced;
   nothing suggests it is the only one, and the guard in (2) will say.

## Knock-on to check, not assume

SQ-1152 shipped `shogun-amiga-extended` (Amiga r295) and argued for omitting
`--v6-pixel-lock` partly from "Shogun has no Macintosh press". That premise is
false. Its OTHER argument stands on its own and should be re-checked rather than
discarded: `RasterFrame::extended` floors to whole DEVICE pixels per NATIVE pixel,
strictly stricter than the pixel lock's per-ART rung, so the SQ-1012/1024 half-rung
problem cannot arise in extended on any press. If that holds, the omission is still
right and only its stated reason was wrong.

Related: [[SQ-1152]] is the capture; SQ-1012/SQ-1024 are the half-rung quests.

--- note 2026-08-30T21:53:17Z ---

DONE on branch (Lane A of wave 3), commit 9b5bb5b9 `test(media): the table now says what it does not know, and a guard makes it`.

## The press, measured

`stories/Shogun.toast`, mounted through `app::hints::load_mounted_story` like every other row in the table: **v6, release 292, serial 890314**, `DiskImage::Hfs`. That is a FOURTH Shogun build on a FIFTH medium, and the earliest of the four — Amiga r295/890321, Apple II r311/890510 (five .dsk floppies and Shogun.po), bare file r322/890706. Nothing was copied from a sibling. It agrees with three suites that already used the file (`v6_hint_clue_wrap.rs`, `v6_side_border_tiling.rs`, `v6_extended_frame.rs`, `v6_raster_text_loss.rs` all name 292/890314), so the release was known to the corpus and only the release TABLE had never heard of it.

Also worth recording: `.toast` was already in `blorb::medium`'s Hfs extension census (`["image", "bin", "iso", "dc42", "toast"]`), so the picker had been offering the file all along. Nothing needed changing outside the test suite and the doc.

## The guard, and what it turned up

`every_disk_image_in_stories_is_pinned_or_explained` walks `stories/`, asks `DiskImage::detect` of each file, and fails on any recognised medium that neither `MEDIA` pins nor a new `UNPINNED` table explains. Skips vacuously when `stories/` is absent; falsified by deleting the Shogun row (fails with `Shogun.toast — Hfs, v6 release 292 serial 890314`, the symptom as reported).

Shogun was NOT alone — the guard named **twenty-one further media**, every one mountable and none in the table:

- **Atari ST (6):** Compilation 1 Stationfall v3 r107/870430 · 2 A Mind Forever Voyaging v4 r77/850814 · 3 Border Zone v5 r9/871008 · 4 Bureaucracy v4 r116/870602 · 7 Spellbreaker v3 r87/860904 · 5 Trinity v4 r11/860509 (the same build Compilation 8 opens with). Three of nine volumes had rows; nine do now.
- **DOS (9):** `floppy1.ima` Beyond Zork v5 r57/871221, `floppy3.ima` The Lurking Horror v3 r203/870506, `floppy4.ima` Stationfall v3 r107/870430 (all five `floppy*.ima` are named now); `disk1.img` AMFV v4 r77/850814, `disk2.img` Trinity v4 r12/860926, `disk3.img` Bureaucracy v4 r116/870602, `disk4.img` Sherlock v5 r21/871214 (four 720 KB volumes labelled DISK 1–4, several stories each, the row is the one that opens); and two standalone Zork Zero pressings, the 360K three-disk set and the 720K two-disk set, both v6 r393/890714.
- **Macintosh (2 more):** `Sherlock.img` v5 r26/880127, and `InfocomMasterpieces.img` — the Masterpieces CD mounts as the 12 MB HFS volume it is and opens with Zork Zero v6 **r296/881019**, the SAME build as `Zork Zero Disk.image`. So the October-1988 outlier is the Macintosh's build, not one disk's.
- **Amiga (1):** `Sherlock - The Riddle of the Crown Jewels.adf` v5 r26/880127 — the same build as the bare `sherlock-r26-s880127.z5`, so Sherlock joins PAIRS as a `true`. Its DOS and Apple IIgs presses are r21/871214, a different build: one title making both halves of the rule.
- **Commodore (1):** `zork_ii[infocom_1984](r88)(!).g64` — the filename says r88 (that is ZORK I's release) and the header says **v3 release 48 / serial 840904**. Same build as the Amiga floppy and the bare `.z5`. A name is not evidence, on a file named after the wrong game.
- **UNPINNED with a written reason (2):** `Journey.2mg` (declares five segments, carries four — no whole build to pin) and `Lost Treasures … (Disk 1 of 7).2mg` (the GS/OS launcher, no game on it).

## Two things the guard's DESIGN turned up

1. **The fold rule had to be tightened, and tightening it surfaced eight of the twenty-one.** A first version exempted any volume sharing a `disk_set` group with a pinned file. That is too loose: `app::disk_set` groups by filename stem plus digit run, so the nine unrelated `Infocom Compilation N (19xx)(-).st` images are one "release" by that measure, as are `disk1.img`…`disk4.img` (four DIFFERENT games) and `floppy1..5.ima`. The shipped rule folds a volume only when the story off it is the same version/release/serial as the pinned sibling's row. What still folds is exactly the genuine continuation volumes: `journey_s2..s5`, `shogun_s2..s5`, `zork_zero_2..4`, the Zork Zero DOS disks 2/3.

2. **Observation, not a defect I fixed:** because of that same grouping, opening any one of the nine ST compilations offers the union of the set — `zvm-cli "Infocom Compilation 8 …"` lists **33 stories**, mixing that disk's own `LURKING/STORY.DAT` with `HITCHHI.T` off its "siblings". The per-file tiebreak still resolves to the volume's own largest story (each of the nine opens a different game), so no row here is wrong, but a nine-disk survey being treated as one multi-volume release is worth knowing. Left alone — it is `cli-host`/`blorb` territory, outside this lane.

## Knock-on named in the quest

The SQ-1152 premise "Shogun has no Macintosh press" is false and the row now proves it. Its OTHER argument was not re-checked here (out of lane): `RasterFrame::extended` flooring to whole device pixels per NATIVE pixel is strictly stricter than the pixel lock's per-ART rung. If that holds the omission of `--v6-pixel-lock` stands on its own and only its stated reason was wrong.

Gates: `cargo check --all-targets` clean; `cargo nextest run -p app real_media_releases` 12/12 pass (including the picker, profile, narration and v6-render cases against 22 new rows); `cargo clippy -p app --all-targets -- -D warnings` clean. Per the wave brief, no full workspace gate — CI is the backstop.
