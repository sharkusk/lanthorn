---
title: 'Kerkerkruip: restoring a Save State paints the gap between the first two panels with the terminal''s black background, with some bleed-through in the lower right as well, instead of the game''s window background'
status: open
type: bug
priority: low
created: 2026-09-13T15:02:14Z
commits: []
context: |-
  branch: main
  head: a78ff4d0
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1463

  User report 2026-09-13 playing Kerkerkruip on v0.6.1; a restore reconciles the saved Glulx window layout with the current pane and leaves cells between windows unpainted.
---

--- note 2026-09-15T15:30:52Z ---

2026-09-15: user can only reproduce this on their Linux machine, not the macOS dev machine. Held — do not dispatch further work until they can reproduce it here. An investigative lane was stopped mid-task for the same reason; whatever it found is in its worktree, not yet merged.

--- note 2026-09-15T15:32:47Z ---

2026-09-15 investigation (stopped before reproducing, per hold): real archive is ~/.lanthorn/saves/Kerkerkruip.gblorb.save/default.lanthorn (turns=1, command_history=["panels"], location Entrance Hall), not the tracked stories/Kerkerkruip.gblorb.save/ which is only the game's own Glk VFS. Leading theory: Glulx's restore path (glulx_session.rs restore_state) has no counterpart to session.rs's reconcile_restored_screen_size (Z-machine only) — apply_deferred_resize() only replays a resize queued during an open dialog, not a snapshot-vs-current-pane mismatch. If the snapshot's Glk layout was taken at a different pane size, screen_model()'s content_size stays the stale rect, clamping the composite and leaving bottom/right strips to fill_margin — would explain both the black gap and the lower-right bleed from one cause. Separate candidate for the bleed alone: a stale kitty placement from Kerkerkruip's own divider graphics window surviving the restore's id churn.
Next step, doesn't need local repro: ask user for /dump-windows + /dump-cells output right after a restore on the Linux machine, plus the live pane size — /dump-windows' rect vs actual pane size settles the clamp theory directly.

--- note 2026-09-15T15:34:41Z ---

2026-09-15: user tested restoring at various terminal sizes and the layout adjusts correctly after loading — so the "no reconcile_restored_screen_size analogue for Glulx" theory above is RULED OUT, some other path already handles the resize correctly. Do not re-investigate that lead. Still held pending local reproduction; the actual cause of the black gap/bleed-through is unknown.

--- note 2026-09-15T23:19:40Z ---

2026-09-15: root cause found, confirmed by direct measurement, NOT platform/backend-specific. AppGlk's grid/buffer/graphics content is keyed by Glk window id; restore swaps in gvm's window model but never reconnects backend content, so an archive whose window ids don't match the live session (any archive taken after a prior restore/restart/panel-rebuild, since ids climb monotonically) leaves every non-primary window empty. Only the window's declared background colour survives (correctly round-trips through gvm), producing exactly the reported flat-colour symptom. Proven via A/B/C: same-session restore (ids match, fine), archive into a fresh session (ids collide, blank), user's real archive into the session it came from (ids match, looks right but shows stale content, not the actually-restored state — same shape as SQ-0901/1020/1022's self-consistent-but-wrong numbers).

Bigger finding: Glulx has NO screen-state persistence in the archive at all — zvm writes screen.bin, Glulx writes nothing equivalent. Violates the project's own persistence rule. Three fix directions (see full lane report in session history): (1) real Glk screen-state persistence, needs a Glk graphics display-list concept first since AppGlk only holds a rendered canvas today; (2) clear+push evtype_Redraw on mismatch, honest but leaves text panels blank since Glk can't ask a game to reprint a window; (3) re-key old content onto new ids, wrong. User needs to decide direction before dispatching a fix. Diagnostic harness and full captures saved outside the worktree at the session scratchpad (repro-harness.rs, diag.txt, diag2.txt, diag3.txt) since nothing was committed.
