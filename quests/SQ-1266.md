---
title: 'advent.z6: a host snapshot taken from one GameSession fails to restore into a second session of the same story bytes with SaveMismatch (the game randomises its release banner at boot) — Quetzal cross-instance restore should never depend on runtime state; investigate whether zvm''s mismatch check reads something the game rewrites'
status: open
type: bug
priority: low
created: 2026-09-03T16:22:54Z
commits: []
context: |-
  branch: main
  head: b598420c
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1113

  Found by the SQ-1264 lane (2026-09-03) while proving Phase 2 on advent.z6: the shadow probe's restore of a live snapshot fails with SaveMismatch on that fixture only, so the reseeded shadow-walk path is untestable there; the lane documented it as a fixture quirk, but a save from identical story bytes failing to restore into a fresh instance is a zvm-side question worth answering before the shadow machinery is trusted on every game.
---

--- note 2026-09-03T16:30:54Z ---

2026-09-03: not reproducible. User could not reproduce in the app; a headless check on main (b598420c) booting stories/advent.z6 twice from the same bytes at 25x80, playing ""/""/look/west on the live one, then `restore_state` of its `save_state()` into the second instance returns Ok and the second instance reports "At Hill In Road" on `look`; same-instance restore also Ok; advent.z8 likewise Ok. The SQ-1264 lane's SaveMismatch was therefore an artefact of its own harness (most likely two boots with differing screen geometry or a snapshot taken mid-intro), not a zvm defect. Discarded.

--- note 2026-09-03T19:40:55Z ---

REOPENED 2026-09-03: a second, independent lane (SQ-1269) reports the same thing in the same place — on advent.z6 the ShadowProbe's restore of a live snapshot fails so consistently that its Suspicion path had to treat `Answer::run == None` as the ordinary case there. My earlier direct check (GameSession::save_state → restore_state into a second GameSession at the same 25x80) succeeds, so the failure is specific to the SHADOW's path: the shadow boots from `ShadowRecipe` (its own geometry/profile/screen args) rather than the live session's `new_with_trace(... Some((25,80)))`, and a v6 host snapshot carries screen state — the mismatch is most likely there (screen size / interpreter profile / palette in the EngineSave vs the shadow's boot), not in Quetzal. Consequence: the return probe and every Phase 2 probe silently never run on advent.z6 (and possibly on every v6 story). Needs an investigation with the exact error captured from the worker.
