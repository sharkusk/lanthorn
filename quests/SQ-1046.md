---
title: Leaving Arthur's F5 screen leaves all game text coloured, and win2 stays a text window
status: discarded
type: bug
priority: low
created: 2026-08-24T02:18:01Z
commits: []
context: |-
  branch: main
  head: ee962412
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1009

  Reported by the user testing SQ-1009's build; the engine-pen lane demonstrated the engine holds no colour at any point and reproduced the same end state with the pre-SQ-1009 metric, so it is pre-existing rather than that lane's.
tags:
  release: "0.3"
---

--- note 2026-08-24T02:18:21Z ---

## The report

Enter Arthur's F5 description screen, leave it, and **all the game text comes back
coloured rather than white**. Amiga press, raster and hybrid.

## Established: the engine is not holding a colour

The SQ-1009 engine-pen lane checked this before returning it. At every point —
before F5, inside F5, after leaving F5, and after the next `look` — `zvm` reports
`current_fg` and `current_bg` as `Default`, and **every window** as `fg=Default
bg=Default colour_data=0x0000`. So no `set_colour` is outstanding and nothing in
the engine's colour state explains it.

## Established: not a SQ-1009 regression

Reproduced identically with `TextFace` forced to hand zvm a `V6Metric::fixed`,
which is the pre-SQ-1009 engine exactly: same end state, `attrs=1001`, 29 painted
runs, all colours `Default`. So it is present at `ee962412` and older.

## The lead

**`win2` stays `attrs=1001` with its painted runs after F5 exits.**

F5 turns win2 — normally the PICTURE window — into a text window: attributes go
`1000 -> 1001` and the model goes `Graphics -> Grid` (both captures are in
`~/.lanthorn/dump-windows.log`, timestamps `22:51:27` and `22:51:33`). Leaving F5
does not appear to take it back, so a window that should be graphics again is
still a grid full of retired text runs, and the renderer is colouring text against
whatever that window's state implies.

So look **downstream of the window-kind change and at paint lifetime**, not at
colour state:

* what, if anything, is supposed to return win2 to `attrs=1000` on leaving F5 —
  the game, or us reading it wrongly;
* whether win2's painted runs are being retired when it stops being a text window
  (v6 text is PAINT: a run stays where it was put until painted over or erased —
  `V6Windows::paint_run` / `erase_screen_rect`);
* which layer actually chooses the ink for the story text once win2 is in that
  state, since the engine is handing it `Default`.

## Reproduction

Amiga Arthur (`stories/Arthur - The Quest for Excalibur.adf`, release 54 serial
890606). Reach the churchyard, press F5, leave F5, then type `look`. Capture
`/dump-windows` at each step — the two already in the log are the before and the
during.

Related: SQ-1009 (which surfaced it and cleared itself), and CLAUDE.md's note that
v6 text is paint and survives `move_window`/`window_size`.

--- note 2026-08-24T05:41:18Z ---

## The colour symptom does not reproduce — and the documented repro was wrong about the exit

Amiga Arthur (release 54 / serial 890606), 15 blank turns + `look` to the churchyard, then F5.

**F5 is not a modal screen.** After F5 the pending input is `Line` — the game is at its ordinary prompt with the description painted into win2. There is nothing to "press to leave"; you leave by choosing another view. Sweeping the function keys from that state:

| key | win2 after |
|---|---|
| F1 | `attrs=1000`, 0 texts — **the picture view; this is the exit** |
| F2 | `attrs=1000`, 0 texts |
| F3 | `attrs=1000`, 23 texts (inventory) |
| F4 | `attrs=1000`, 20 texts (score) |
| F5 | `attrs=1001`, 28 texts (description) |
| F6–F9 | `attrs=1001`, **0 texts** |

**On the F5 → F1 round trip everything is correct.** win2 returns to `attrs=1000` with zero texts; the story ink is `Rgb(255,255,255)` before and after in hybrid, and the raster composite is pure white ink on a `[66,66,66]` ground before (9057 inked px) and after (9554). Measured in both `v6_render` modes with `honor_game_colours = true` and the Amiga palette the profile resolves. So the earlier "win2 stays attrs=1001" observation was an artefact of never pressing F1 — the log's two captures are "before" and "during", and there was no "after".

**Needed from the user: which key they pressed to leave F5.** That is the only variable left; every route I can drive is clean.

## A real adjacent defect, found while sweeping — worth its own quest

After F5, pressing **F6–F9 leaves win2 at `attrs=1001` with zero painted runs**. A wrapping, scrolling, empty window is then classified as a TEXT window rather than Graphics, the picture panel disappears from the layout, and the whole frame collapses upward: the score bar moves from row 13 to row 0 and the prose from row 19 to row 3, with the raster composite's inked pixel count falling from 9057 to 553. Whether the game really means to set wrapping there — or whether F6–F9 are unbound on this release and we are mis-delivering a terminating character — is unresolved. Check against a reference rendition before changing engine behaviour.
