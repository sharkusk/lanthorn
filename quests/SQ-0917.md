---
title: Adopt each release's own font metrics for v6 instead of the fixed 8x16 cell
status: partial
type: feature
priority: low
created: 2026-08-18T22:13:08Z
commits:
  - db47f58df33f92a903dcfcea5583bab3643873e4
  - 1b089aca952bb6a43ef395ac45a90e2e53836f0a
  - a4b8efea993be50505493f3979124b322aea6695
  - 878b7c0f2b4c5da193f7aa712ade5eb52f52e291
  - 3296c212efdc5db4081f2bd3abc21302246a8ee1
  - 0469a9366c6614e8739cce9d3a149d120c707b68
context: |-
  branch: main
  head: 013d81c5
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-0911

  Closing out SQ-0916/SQ-0911: the shipped fonts now parse, but drawing with them needs their cell size too — Mac FONT 524 advances 7 against our 8 — and the user's read is that this makes more sense for a future GUI than for the TUI.
---

--- note 2026-08-18T22:13:35Z ---

## What this is, and why it is not the TUI's fight

babelmap advertises `V6_FONT_WIDTH = 8` / `V6_FONT_HEIGHT = 16` at header `$26`/`$27` and lays every v6 cell out on that grid. The machines Infocom shipped for did not:

| release | font | cell | advance |
|---|---|---|---|
| Macintosh v6 (all four) | `FONT` 524 | 7×15 | **7**, uniform over printable ASCII |
| Macintosh v6 (all four) | `FONT` 1033 | 7×12 | 6 |
| Amiga *Arthur* | `char.data` | 10×10 | **proportional, 2–8** |
| Amiga *Journey* / *Beyond Zork* | `Char.data` | 8×8 | 8 (this one is font 3, not a typeface) |

SQ-0911 and SQ-0916 landed the parsers for all of these — `blorb::{bitmap_font, amiga_font, mac_font, resource_fork}` and `Hfs::read_resource` — and then deliberately did NOT wire them to the renderer, because glyphs alone are not enough. Drawing a 7-advance face on an 8px grid costs a pixel of tracking on every character; the comparison renders under SQ-0916 show it, and it is not fixable by choosing better glyphs.

## Why it is genuinely hard, not merely tedious

`$26`/`$27` are not decoration. **The game reads them** and computes its own window geometry from them, so changing what we advertise changes where the story puts things — this is not a rendering-only change, it is a change to the conversation with the story file. On top of that the entire v6 test corpus is pinned at 8×16, and v6 geometry is the area of this repo with the worst track record (SQ-0901's fabricated Arthur frame, the ceil-vs-round trap in CLAUDE.md).

Arthur's font is worse still: it is genuinely proportional, so honouring it means abandoning a fixed cell for text altogether, which the Z-machine's own column arithmetic does not really support.

## Why a GUI is the better home (user's call, 2026-08-18)

The user's framing: *"we may not do it for this tui, but if we build a gui later it will make more sense."* That matches the earlier brainstorm in the same session about a separate application dedicated to playing Infocom v3–v6 as authentically as each platform had them — C64/C128, Apple II/IIgs, Macintosh, Amiga, DOS — with that machine's palette, resolution and system font. In a GUI there is no terminal cell to fight: the window is pixels, the font's own metrics are simply the metrics, and a proportional face is not a problem to be worked around.

In the TUI the cell is the terminal's, and every gain here is fighting that.

## What already exists to build on

- `blorb::bitmap_font::BitmapFont` / `Glyph` — one shape for both machines' fonts, rows MSB-leftmost, advance per glyph.
- `blorb::amiga_font::parse` / `from_volume` — AmigaDOS disk fonts, signature-matched (`DFH_ID`), never by filename.
- `blorb::mac_font::parse` / `from_fork` — `FONT`/`NFNT` including the left side bearing, which is the part that is easy to drop and makes uniform text look ragged.
- `blorb::resource_fork::ResourceFork` and `Hfs::read_resource` — SQ-0911.
- `machine-screenshots/` — the only oracle that can falsify a geometry claim rather than confirm it. It has Amiga Journey, Shogun, Zork Zero and Beyond Zork; **no Arthur**, which is the release whose metrics are most unusual.

## If it is ever picked up here rather than in a GUI

Start from the Macintosh, not the Amiga: `FONT` 524 is fixed-pitch over printable ASCII, so it needs a cell-size change and nothing else. Arthur needs proportional text layout, which is a different and much larger problem. And capture an Amiga Arthur screenshot first — pinning its geometry without one is exactly how SQ-0901 happened.

--- note 2026-08-20T16:12:52Z ---

## A user-visible symptom, which this quest has never had before (2026-08-20)

Reported while testing SQ-0934's hint-screen work: on the Macintosh **black and
white** press, the hint system's rendered glyphs do not fit the upper banner —
they overrun it.

That is this quest's cell, showing on screen for the first time. Every previous
argument here was about fidelity in the abstract, and `v6_font_cell` declined the
change three times (EGA SQ-0790, Macintosh SQ-0838, Apple IIgs SQ-0857) on the
reasoning that nothing actually depended on it. Something does now.

### The arithmetic

The monochrome archive (`Pic.data`) puts the Mac on a **480x300** screen — that is
`ge.mono`, 1x, and `PictSource::native_std_window` carries it to the story ahead of
the profile. lanthorn advertises an **8x16** cell, so the game is told it has

    480 / 8 = 60 columns

where Infocom's own Macintosh interpreter set `colWidth := 7` and
`lineHeight := 15 {16}`, giving the machine

    480 / 7 = 68 columns

So the story lays its banner out for 68 columns and is handed 60. Eight columns of
text have nowhere to go, and a banner is exactly where that shows first: it is a
fixed-width strip with its content positioned by column, unlike prose, which simply
rewraps.

Note this is the MONO press specifically. The colour press takes `CPic.data` at 2x
onto a 640x400 screen, where 640/8 = 80 against the Mac's own 640/7 = 91 — the same
ratio, but the banner there has more absolute room to absorb it, which is why the
b/w one is the press that broke first.

### What this changes about the quest

Nothing about the difficulty — `$26`/`$27` are still read by the story, the whole
v6 corpus is still pinned at 8x16, and Arthur's Amiga face is still proportional and
still belongs in a GUI. But the "start from the Macintosh" advice at the bottom of
this quest is now the *recommended* path rather than merely the easiest one, and it
has a falsifiable acceptance test at last: the Mac b/w hint banner fits.

`FONT` 524 is fixed-pitch over printable ASCII, so the Macintosh needs a cell-size
change and nothing else. And SQ-0932 already landed the glyph half of this quest for
the raster path, so what remains for the Mac really is only the cell.

### Related

- SQ-0934 — the hint screen the symptom was found on.
- SQ-0937 — a DIFFERENT Mac-only hint defect found in the same session (an in-box
  run's column is device-mapped while its row is box-relative). Do not conflate
  them: SQ-0937 is a rounding boundary in the ring's run packing and is present at
  8x16; this one is the cell itself. Fixing SQ-0937 will not make the banner fit.

--- note 2026-08-22T18:02:06Z ---

## The Amiga Arthur screenshot exists now, and it confirms the font (2026-08-22)

`machine-screenshots/amiga-arthur.png` — Arthur on an Amiga, gameplay frame
("Town square / St John's Day, Terce"). This is the capture this quest and
SQ-0901's lesson both asked for and did not have.

**It is a pixel-exact native capture, not a resample.** 320x200, 18 distinct
colours, and every channel of every colour is a multiple of 0x11 (0x44, 0x55,
0x99, 0xDD...) — a 4-bit-per-channel OCS palette with no blended values
anywhere. A downscaled or video-captured frame shows off-palette averages; this
one has none, so every measurement below is of real machine pixels.

### The cell is 10 tall, measured

White text pixel rows: 121-129, 131-139, 141-149, 161-169, 171-179. **Pitch
exactly 10.** The status bar (inverse, white ground) occupies rows 100-109 —
exactly one cell tall, independently. Glyphs use ~7 rows with descenders
reaching the 9th, inside a 10-row cell.

### The advance is proportional, measured per word

Prose line 1 is "You walk into the town square. The churchyard lies to the
north," — 64 characters spanning x=14..301, **288 px, 4.5 px/char average**.
Fixed-pitch at the nominal 10 would need 640 px on a 320-px screen, so
proportional is not a detail here; it is the only way the line fits.

Segmenting that line by inter-word gaps and dividing each word's span by its
letter count:

| word | span | px/char |
|---|---|---|
| You | 15 | 5.00 |
| walk | 20 | 5.00 |
| into | 16 | 4.00 |
| the | 13 | 4.33 |
| town | 21 | 5.25 |
| square. | 32 | 4.57 |
| churchyard | 50 | 5.00 |
| lies | 15 | 3.75 |
| to | 8 | 4.00 |
| the | 13 | 4.33 |
| north, | 26 | 4.33 |

A fixed-pitch face gives every word the SAME px/char. These range 3.75-5.25 and
track word content (`lies`, all narrow letters, lowest; `town`, with a `w`,
highest). That is the falsification, not merely a consistency check.

So `native_disk_font.rs`'s `arthur_carries_a_proportional_text_font` — 10x10
nominal, `FPF_PROPORTIONAL` set, advance 2..=8 from `char.data` on
`Arthur - The Quest for Excalibur.adf` (r54/890606) — now agrees with a real
machine and not only with its own parser. **Arthur's Amiga text was never
monospaced, confirmed externally.**

### The frame's band structure, for whoever pins geometry against this

At 320x200: rows 0-6 grey ground; 7-90 the knotwork border and illustration;
91-99 grey; **100-109 the inverse status bar (one cell)**; 110-120 grey;
121-179 prose at 10px pitch; 190-199 the input line. A green vertical rule runs
down x~314-319 the full height.

### A SEPARATE finding this raises — text size relative to art

`AMIGA_STD_WINDOW` is (320, 200) and `V6_ART_SCALE` doubles it to the 640x400
unit screen, where the 8x16 cell gives 80x25 characters. The machine in this
capture has **20 text rows** (200/10), not 25.

Expressed as a ratio, which is the part that does not depend on the doubling:
lanthorn's line is 16/400 = 4.0% of screen height; the Amiga's is 10/200 =
5.0%. **Real Amiga v6 text is ~25% larger relative to its artwork than
lanthorn draws it.** Columns are closer — the machine fits ~71 columns of
average-width text across 320 px against lanthorn's 80 — so the vertical is
where the doubling model actually costs something.

This is NOT a defect claim and should not be actioned from this note alone: the
2x unit-screen model is deliberate (SQ-0479) and Arthur's windows are laid out
from what we tell it, so a taller grid is not automatically a wrong one. It is
recorded because it is now measurable against a machine, which it was not
before, and because it says the Macintosh is the outlier by a wide margin while
the Amiga's model is merely a bit small.

### What it does NOT change

Arthur still belongs in a GUI as far as GLYPHS go. Adopting a proportional face
means `x_cursor += V6_FONT_WIDTH` becomes `x_cursor += advance(ch)` and the
$20/$21 character grid stops meaning anything — a far deeper change than making
the cell runtime state — and hybrid can never follow it, so raster and hybrid
would place the same game's text differently. The screenshot removes the
"unfalsifiable" objection to Arthur; it does not remove the structural one.

--- note 2026-08-22T18:27:09Z ---

## The Macintosh cell is confirmed against a real machine — and the Mac draws PROPORTIONALLY (2026-08-22)

Two new captures, `machine-screenshots/mac-zorkzero-game.png` and
`mac-zorkzero-hint.png`, from an emulator running the **black-and-white**
Macintosh press — the exact press this quest's symptom was reported on.

**Both are trustworthy in a way the earlier `mac-*.jpg` files are not.** PNG, 1-bit
(the hint capture has literally TWO colours), and the uniform outer border resolves
to a content rect of exactly **512x342** — a compact Macintosh screen (Plus / SE /
Classic) with a 20 px emulator frame. 1:1, unscaled, nothing to infer.

For contrast, `mac-arthur.jpg` cannot be pinned from: lossy, windowed, and its
advance/line-height ratio comes out 5.6/14 = 0.40 where Mac's own colWidth/lineHeight
is 7/15 = 0.47 and ours is 8/16 = 0.50. Something is scaled by an unknown factor.
**Do not measure metrics from the JPEGs.**

### The screen is 480x300 — confirmed

Window frame inside the 512x342 desktop: horizontal rules at desktop-relative rows
19 (top), 23-37 (title bar), 338-339 (bottom); vertical borders at columns 11 and
500-501. Content is **exactly 300 rows tall**, ~480 wide after the inner inset.

That is `Pic.data`'s 480x300 (SQ-0838), verified externally for the first time.
`PictSource::native_std_window` is right.

### The line height is 15 — confirmed three independent ways

1. The inverse highlight bar on `PROLOGUE` spans rows 118..132 = **exactly 15 rows**.
   An inverse bar is drawn to the cell, so this is the cell, not an ink measurement.
2. All fifteen topic rows land on `y = 118 + 15*i` — a formula that indexed 14
   consecutive lines correctly.
3. Banner baselines are 15 apart (84 → 99).

And the same 15 px pitch appears in the GAMEPLAY capture's prose (line tops at 241,
256, 271, 285), so it is not a hint-screen peculiarity.

Infocom's Mac source said `lineHeight := 15 {16}`. It is 15.

### **The drawn face is PROPORTIONAL. This is the finding.**

The decisive test controls for glyph identity: `WING` is the same four-glyph run in
three different topics.

| topic | char index of `W` | x of `W` | ink width of `WING` |
|---|---|---|---|
| `EAST WING` | 5 | **137** | 30 |
| `WEST WING` | 5 | **139** | 30 |
| `SECRET WING` | 7 | 151 | 30 |

Same glyph run, same character index 5, **different x** — 137 against 139 — because
`WEST` is 2 px wider than `EAST` (`W` wider than `A`). Under ANY fixed pitch those two
must be identical. The measurement is sound in both directions: `WING` inks to exactly
30 px in all three rows and `AREA` to exactly 31 px in all three of its rows, so
identical strings do have identical widths.

**But the average advance is 7.00.** `WING` at index 5 (x=137) versus index 7 (x=151)
is 14 px over 2 characters — exactly 7.000. `BAILEY/VILLAGE AREA` puts `AREA` at index
15, x=207, i.e. 106 px over 15 characters = 7.07.

### Why that resolves the apparent contradiction with `native_disk_font.rs`

Infocom's Mac interpreter reads `stdFont := geneva; textSize (12); lineHeight := 15
{16}; colWidth := 7`. **Geneva 12 is a proportional System font.** So the Mac
interpreter DRAWS proportional Geneva and DECLARES a nominal column width of 7 to the
story — and 7 is simply Geneva 12's average advance, which is why the two agree.

`colWidth := 7` was never a drawn advance. It is a declared metric, and this quest's
header `$27` is exactly the same quantity.

### **This SIMPLIFIES the quest: the Macintosh needs the cell and NOT the glyphs**

The right change for the Mac is the declared 7x15 and nothing else. Adopting the
drawn face would mean proportional rendering — the Arthur problem — and would be
*wrong on its own terms*, because the real interpreter did not tell the story about
proportional widths either. It told it 7. Matching Infocom's declared metric is both
the easier change and the more faithful one.

So the earlier "start from the Macintosh" advice stands and gets cheaper: no glyph
work, no `FONT` 524 wiring, one profile returning `(7, 15)`.

### A second, smaller face on the banner — chrome, not a Z-machine font

The InvisiClues banner ("Hit N for next item." / "Hit Return to see hints.") is drawn
in a **different, smaller proportional face**: ~6.3 px average advance against the
topic list's 7.0. Same-string widths are consistent there too (`Hit` inks to 17 px in
all four occurrences, `to` to 11 px in both), so this is a real second face and not
measurement noise. Presumably Geneva 9 / `FONT` 1033 (the quest's table: 7x12,
advance 6).

**The Z-machine cannot express this.** Fonts 1-4 are normal / picture / character
graphics / fixed-pitch; there is no "smaller face". So the Mac interpreter chose it
for its own chrome, and lanthorn should NOT try to reproduce it.

**This tempers the acceptance test.** "The b/w hint banner fits at 7x15" is not
guaranteed by these measurements, because the machine draws that banner at ~6.3 px
and lanthorn will draw it at 7. Going 8 → 7 removes most of the overrun but may not
remove all of it. Reproduce the banner against `mac-zorkzero-hint.png` before
declaring the symptom fixed, and if a residual overrun survives at 7, that is the
second face and a separate question — not a failure of the cell change.

### Also added: `amiga-arthur-hint.png`

Arthur's Amiga InvisiClues screen, 720x569. Text pitch measures **20 px**, i.e. 2x the
10 px cell measured on `amiga-arthur.png`, so this capture is a doubled rendition of
the same 320x200 screen rather than a native hires one — usable for layout and
z-order, but normalise before quoting metrics from it. Its value is as an external
check on **SQ-1004** (the Amiga hint screen painting islands), which shipped on
internal measurement only.

--- note 2026-08-22T19:18:12Z ---

## `V6Cell` is the seam a GUI would need, and that changes what this quest IS (2026-08-22)

Raised while threading the cell: if `zvm` is ever driven by a GUI, where
proportional text is the norm, how does that work — and is this refactor a step
toward it or away from it?

It is a step toward it, and the same sites.

### `gvm` has no equivalent problem, and the contrast is the answer

Glk was designed so the story never learns glyph widths. `wintype_TextBuffer` is
a stream — you print, the library lays out — and `glk_window_get_size` returns
only an approximation in characters; a story that needs a grid asks for
`wintype_TextGrid` and gets one. So Glulx hosts render proportionally for free.

The Version 6 Z-machine is the opposite: it LEAKS pixel metrics to the story
(`$26`/`$27`, pixel cursors, `@move_window` in pixels). That is the whole reason
this is a `zvm` question and never a `gvm` one, and it is worth remembering
before assuming a fix belongs in both engines.

### What v6 already provides for proportional text

Three affordances, and they look deliberate rather than accidental:

- **`$30` total width.** Print to stream 3 with a width operand, deselect, read
  `$30`. That is the spec's "how wide is this string" channel and Infocom used
  it — Shogun right-aligns its status fields that way. A proportional host
  answers with real summed advances; `wrap_stream3_text` currently answers
  `chars * cell.w`.
- **A fixed-pitch escape hatch** — font 4 and the force-fixed-pitch flag — for
  when a story genuinely needs a column grid. The story opts INTO a grid rather
  than the interpreter pretending everything is one.
- **Per-window font size**, queryable through `@get_wind_prop`, so metrics are
  not even global to the screen.

**TO VERIFY BEFORE BUILDING, not from memory:** ZMSD §8.1.1's definition of `$27`
for a variable-pitch font. The recollection is that it is the width of the `0`
character, which would make "declare a nominal, draw proportionally" the spec's
INTENDED reading rather than a fudge — and would retroactively explain Infocom's
Macintosh declaring 7 while drawing proportional Geneva 12. That is a
from-memory claim and this repo does not take those (CLAUDE.md). Check it.

### The shape a GUI needs

Today the fixed cell is baked into the ENGINE: `x_cursor += cell.w`, `px_w() =
chars * cell.w`, `wrap_stream3_text` computing `n * fw`. A proportional host
needs `zvm` to ASK rather than assume:

```rust
trait FontMetrics {
    fn advance(&self, ch: char, font: u8, style: u8) -> u16;
    fn measure(&self, s: &str, font: u8, style: u8) -> u16;  // NOT sum(advance) — kerning
    fn line_height(&self, font: u8) -> u16;
}
```

`Machine` holds one; the default is a fixed cell.

### A correction to this quest's earlier reasoning

An earlier note here argued that proportional advance in `zvm` would make the
engine's model render-mode-dependent and therefore violate the crate's
no-I/O-policy rule. **That is too strong and should not be relied on.** A metrics
interface SUPPLIED BY THE HOST is a parameter, not I/O policy — the engine stays
pure because it never decides, it asks. A TUI host passes a fixed-cell
implementation and hybrid keeps working; a GUI host passes a font-backed one. Two
hosts, two metric sets, one engine, which is exactly what a Macintosh and an
Amiga running the same story already are.

What survives of the objection is narrower and still true: a host that renders in
TERMINAL CELLS must pass fixed metrics, because hybrid maps `native_px / cell.w`
to one terminal column and that collides once run origins stop being cell
multiples. So proportional is a GUI CAPABILITY, not a mode the TUI can toggle
into. That is a smaller claim than "the architecture forbids it".

### Why this matters for the work in flight

`V6Cell` is a degenerate `FontMetrics` — an advance that happens to be constant.
The ~40 engine sites and 27 render functions being threaded a value through are
precisely the sites that would later take a `&dyn FontMetrics`. **Changing a
threaded value's type is mechanical; adding threading where there was none is the
expensive part**, and this quest is paying that cost once.

So do not describe SQ-0917 as "the Macintosh's cell". It is the metrics seam,
opened, with the Macintosh as its first and simplest consumer.

--- note 2026-08-22T19:52:24Z ---

## Step 3 checklist — what must happen in the SAME commit as the value change

Steps 1 and 2 are the inert groundwork (the cell becomes state; the engine and
then the renderer read it; every profile still answers 8x16). Step 3 is the only
one that changes behaviour, and it is NOT a one-line edit. Everything below has
to land together or the Macintosh comes out wrong in a different way.

### 1. `InterpreterProfile::v6_font_cell()` returns `(7, 15)` for `Macintosh`

Every other profile keeps `(8, 16)`, so only the Mac suites can move. Note
`interpreter.rs`'s existing test pins `v6_font_cell()` against
`zvm::screen::V6_FONT_WIDTH`/`V6_FONT_HEIGHT` — that pin is about the DEFAULT and
will need re-aiming at `V6Cell::DEFAULT` rather than deleting.

### 2. Wire the profile's cell into `Machine` BEFORE `set_screen_dims`

`Machine::v6_cell` is `V6Cell::DEFAULT` until a host sets it, and `session.rs`
calls `machine.set_screen_dims(rows, cols)` during construction — which writes
`$22`/`$24`/`$26`/`$27`. Set the cell first or the story is told 8x16 and then
laid out at 7x15. The resolved profile is available: `startup.rs` writes it back
into `cfg.interpreter_profile` from `resolve_with_source`, so it is the same
value `reload.rs` already reads for `period_look`.

Also populate `AppState::v6_cell` from the same place — it defaults to
`V6Cell::DEFAULT` and nothing sets it yet, which is exactly why step 2 is inert.

### 3. `write_screen_dims` must carry PIXELS as primary, not the character grid

This is the trap, and Infocom's own code states the direction:

    totRows := (bottom - top) {DIV lineheight};
    totCols := ((right - left) - (2 * wMarg)) {DIV colWidth};

The grid is derived FROM the window. babelmap goes the other way — `session.rs`
computes cols from pixels, then `write_screen_dims` re-multiplies `cols * cell.w`
back into `$22`. Lossless at 8 (480/8 = 60, 60*8 = 480); **lossy at 7** (480/7 =
68, 68*7 = 476). The window is 480 and the story would be told 476.

Note `wMarg = 4`, measured on both presses: the b/w content rect is 488 px wide
and the colour one 648, each 8 px more than the interpreter's `wx` (480 and 640).
So the column count subtracts 8 before dividing.

### 4. The raster face has to fit a 7x15 cell

`render/vga16.rs` exists precisely because it blits 1:1 at 8x16 with no
resampling on either axis (SQ-0932), and 7x15 ends that. **Measured: cropping is
lossless for 190 of its 194 glyphs.** Zero glyphs use row 16; exactly four use
column 8 — `*` (0x2A), `_` (0x5F), `(c)` (0xA9) and `¶` (0xB6). Underscore is the
only one that will show, because it is a full-width rule and will gain a 1px gap
between runs. Decide crop-vs-second-face deliberately rather than by default.

### 5. A doc sweep for the worked examples

Step 2's mechanical rename left comments of the form `quantize Y by font_h(16)`
and `320/font_h(16) = 20 raster rows` in `render/screen.rs` and
`render/v6_layout.rs`. They are ACCURATE TODAY — worked examples at the default
cell — and become wrong for the Macintosh the moment step 3 lands. Sweep them
then, not before; there are about eight.

### 6. Acceptance, and its limit

The falsifiable check is the SQ-0934 symptom: the b/w hint banner fits. Reproduce
against `machine-screenshots/mac-zorkzero-hint.png`.

**But do not promise it outright.** That banner is drawn on the real machine in a
SECOND, smaller proportional face (~6.3 px average against the topic list's 7.0 —
`Hit` inks to 17 px in all four of its occurrences, so the difference is real and
not noise). The Z-machine cannot express a second face — fonts 1-4 are normal,
picture, character graphics and fixed-pitch — so lanthorn will draw that banner
at 7. Going 8 -> 7 removes most of the overrun and may not remove all of it. **A
residual at 7 is that second face, not a failed cell change**, and it is a
separate question.

### Related

- **SQ-1009** — the Amiga's declared metric, split out. Nothing here waits on it.
- Arthur's proportional rendering stays a GUI question; see the `FontMetrics`
  note above for the shape and for the correction to this quest's earlier
  architectural objection.

--- note 2026-08-22T20:04:16Z ---

## Correction to the step-3 checklist: the vga16 crop, glyph by glyph

An earlier note here said cropping `render/vga16.rs` from 8x16 to 7x15 was
lossless for 190 of 194 glyphs, and that **underscore was the one that would
show** because it is a full-width rule and would gain 1px gaps between runs.

**That is wrong.** Underscore is the glyph that crops PERFECTLY. Dumped from the
table, the four glyphs that use column 8 are:

```
U+005F  _        ....... | .        one row, all 8 columns inked
                 ####### | #

U+002A  *        .##..## | .        the 8th column is a single pixel on the
                 ..####. | .        middle bar; columns 0-6 are already a
                 ####### | #        complete symmetric asterisk
                 ..####. | .
                 .##..## | .

U+00A9  (c)      #..##.. | #        the 8th column is the circle's RIGHT WALL,
                 #.#..#. | #        across six rows
                 #.#.... | #

U+00B6  pilcrow  .###### | #        the 8th column is the RIGHT STEM, across
                 ##.##.# | #        ten rows
                 ...##.# | #
```

The reasoning error was assuming a gap appears between cells. It does not: in a
7-wide cell, adjacent glyphs sit at x, x+7, x+14, and a rule that inks all seven
of its columns tiles continuously. The gap would only appear if the cell stayed 8
wide while the ink narrowed to 7 — the opposite of what the crop does.

### The corrected picture

| glyph | crop result |
|---|---|
| 190 others | untouched — no ink in column 8 at all, and none in row 16 |
| `_` | **perfect** — still a continuous rule |
| `*` | **improved, arguably** — the 7-column form is already symmetric; column 8 was an extra pixel on the bar |
| `(c)` | **degrades** — loses the right wall of the circle, which opens it |
| `¶` | **degrades** — loses the right stem |

So it is 192 of 194 clean, and exactly two that lose structure — not the one, and
not the one I named.

### What to do about the two

Neither is common in interactive fiction, but `(c)` is NOT hypothetical: Infocom
banners carry a copyright line, so it will reach the screen on a real boot. Two
glyphs is a small, contained job — **hand-narrow `(c)` and `¶` into seven
columns** rather than accepting an open circle, and keep the mechanical crop for
everything else. That keeps `vga16`'s 1:1 blit property (the whole reason it
exists, SQ-0932) at the new cell size with no resampling on either axis.

Zero glyphs use row 16, so 16 -> 15 is lossless for all 194 with no decision at
all.
