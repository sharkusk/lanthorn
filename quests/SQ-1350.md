---
title: 'sq1266_v6_shadow_restore: the Journey r83 shadow case asks a LINE of a menu-only story'
status: open
type: bug
priority: high
created: 2026-09-06T07:20:47Z
commits: []
context: |-
  branch: main
  head: 00a7c3ff
  cwd: /Volumes/Videos/Source/babelmap

  Found while diagnosing SQ-1348. `cargo nextest run -p lanthorn --test mapper_ui` is red on main at `sq1266_v6_shadow_restore::the_shadow_restores_and_answers_on_journey_r83` ("journey-r83-s890706.z6: the shadow printed something" — run.steps[0].reply is empty). Same root cause as SQ-1348: commit ed121541 (SQ-1270) made `GameSession::submit` route a line at a `read_char` prompt to its FIRST CHARACTER; falsified by disabling that branch, which makes both Journey and Advent pass. But unlike SQ-1348 this one has NO gate-clearing fix: Journey r83 is menu-driven and never takes line input at all — probed 80 consecutive `submit_char(b' ')` presses and `pending_input()` is still `Char`, and `submit("look")` returns an empty reply. `live_at_a_prompt` therefore snapshots a session sitting on a keypress page-turn, and the case's old green came entirely from zvm's SQ-1266 terminator-only fallback feeding 13 into that read_char and turning a page — incidental intro text, never an answer to `look`. The open design question is what `ShadowProbe`/`app::probe::serve` should do when the restored story takes no line input (refuse to arm? report a distinct 'story takes no line' outcome?), and whether Journey is the right second fixture for a case about header damage. Advent.z6 reaches a Line prompt after one blank and is unaffected. Not fixed under SQ-1348 because the fix needs that design call.
---
