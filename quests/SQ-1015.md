---
title: Synthesise redistributable fixtures so CI can test the paths that only real commercial media reach today
status: partial
type: feature
priority: low
created: 2026-08-23T02:15:24Z
commits:
  - 19fc75f68093b5dbff6e8a3a58678b11dc970cbd
  - 5cabe0be48e29c26c9e4c72bcb6c0416a4d9fa0e
context: |-
  branch: main
  head: 9dade069
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1011

  Raised while building SQ-1011's Macintosh font loader: it reads FONT 524 off a gitignored commercial floppy, so every test of it skips vacuously in CI — and a survey found 151 suites in that position, 88 of them v6.
tags:
  release: "0.4"
---

--- note 2026-08-23T02:15:52Z ---

## The size of the hole

**151 suites under `crates/app/tests/suites/` skip vacuously when `stories/` is
absent — 88 of them v6.** `stories/` is gitignored because it is commercial game
files, so on GitHub Actions all 151 report green having asserted nothing. The
local gate covers them; CI does not, and CI is what guards a merge.

That is not a defect to be fixed by committing the games. It is a gap to be
narrowed by making fixtures **we author**, which are redistributable by
construction.

## Why not just download the media in CI

Considered and rejected. Fetching a commercial floppy from a URL is the same act
of copying as committing it, with an extra hop — every Infocom v6 title is still
owned. The technical objections (network flake, a file changing under you, cache
management) are real but secondary; the licensing one is the blocker.

The repo already draws this line correctly: `unit_tests/` holds 56 freely
redistributable fixtures, which is why the Glulx suites test properly in CI while
the v6 ones skip.

## The first one, and the reason this was raised

SQ-1011 reads `FONT` 524 out of a Macintosh floppy's resource fork. A synthetic
**minimal HFS volume carrying a bitmap `FONT`** would exercise the whole path
end to end with no commercial content:

- `blorb::hfs::Hfs::mount` on a real volume structure
- finding the `APPL` entry
- `read_resource` pulling a fork out of a file whose DATA fork is zero bytes —
  the case that reads as an empty file if only data forks are reachable
- `ResourceFork::parse`
- `mac_font::parse` / `from_volume`
- `native_font::resolve`'s `fits` guard (dimensions match, not proportional)
- `blit_glyph_styled` sampling it 1:1

Everything except "does Zork Zero look right", which no CI can answer.

**It does not replace `native_disk_font.rs`.** That suite pins the REAL font —
7x15, baseline 12, 200+ glyphs — and is what proves we read *Infocom's* data
correctly. Synthetic proves the machinery, real media proves the data, and both
are needed. Keep the real ones skipping.

## Survey the rest — this is half the quest

Before building anything, work out which of the 151 are reachable this way. A
first cut of the categories:

- **Disk-image containers.** `blorb` parses HFS, ADF, ProDOS, D64, FAT12, ISO9660,
  2MG, and Infocom's own segmented `.D1`-`.D5`. Each has a mount/enumerate path
  that a small authored image would exercise. `blorb`'s own unit tests may already
  do some of this — check before duplicating.
- **Fonts.** The Amiga side too: `amiga_font::from_volume` reads `char.data` off
  an ADF, and an authored ADF carrying a `DFH_ID` font would cover it.
- **Archives.** `infocom_pics`'s flavours (`Pic.data`, `CPic.data`, `.mg1`, `.cg1`,
  `.eg1`) — a tiny authored archive per flavour would test the readers, though not
  the artwork.
- **Not reachable this way:** anything asserting a specific release's LAYOUT —
  `real_media_releases`, the frame-geometry suites, the profile corpus. Those are
  about particular commercial builds and must stay local-only. **Do not try to
  synthesise those**; a fabricated frame is exactly the SQ-0901 failure mode.

Expect the honest answer to be that a minority of the 151 are convertible. That
is still worth having, and knowing WHICH is worth writing down either way.

## Watch for

- **A synthetic fixture that passes while the real path is broken.** The fixture
  must be built by an independent route, not by round-tripping our own writer
  through our own reader — that tests a mirror. Hand-assemble the bytes, or
  generate them from the format spec, and pin the expected values by hand.
- **Non-vacuity.** Every new case needs a guard that fails if the fixture stops
  being read, or it joins the 151 rather than reducing them.

## Related

- **SQ-1011** — the Macintosh font loader, whose CI blind spot raised this.
- CLAUDE.md's fixture rule, which this extends rather than changes.

--- note 2026-08-26T05:18:05Z ---

## STRATEGY DECIDED: fetch on demand in CI. Do NOT vendor the free fixtures.

Directed by the user 2026-08-25. This SUPERSEDES the recommended step 1 in
`docs/ci-fixture-coverage.md` ("Move the free fixtures" into `unit_tests/`) and
narrows the "Why not just download the media in CI" section at the top of this
quest, which as written reads as a wholesale rejection and is not one.

**The distinction the original note missed.** Its rejection is sound for
COMMERCIAL media — fetching an Infocom floppy from a URL is the same act of
copying as committing it, with an extra hop, and every v6 title is still owned.
It does NOT follow for the freely redistributable subset, where fetching is
exactly what the licences contemplate and vendoring is the more presumptuous act.
So: commercial media stays local-only and its suites keep skipping; the free
subset is FETCHED, not committed.

Anyone reading the top section in future: read it as scoped to bucket B, not as a
verdict on the mechanism.

## What already exists, and what does not

`unit_tests/README.md` is already the right shape — a manifest of SHA-256 + byte
count per file, two upstreams (Zarf's Glulx page and `erkyrath/glk-dev`), and a
`curl` re-fetch recipe. Twenty-odd Glulx conformance stories are covered by it and
are gitignored rather than vendored, which is this strategy already in force for
one corpus.

**But nothing fetches it.** `.github/workflows/test.yml` has no download step at
all: `Install Rust` → `Cache cargo build` → `cargo test` → `cargo clippy`. The
README is a recipe for a human. Its own comment states the consequence plainly —
story-fixture-gated tests "skip themselves cleanly when that directory/file is
absent — which it always is in CI". `crates/gvm-cli/tests/fixtures/glulxercise.ulx`
is the one story vendored, for the in-tree conformance test.

So the remaining work is not "decide whether to fetch" but:

1. **A manifest for the free subset** — the 39 suites' fixtures (`advent.z6`,
   `advent.blb`, `scopa.z6`, `sunburst.z6`, `mysterious01`-`11.z6`, `fmvpoker.z6`,
   `anchor.z8`, `photopia.z5`, `minizork-r34`, and the modern Glulx and Scott
   works), each with an upstream URL, a SHA-256 and a byte count, in the shape
   `unit_tests/README.md` already uses. Verified individually, since "freeware"
   still is not "any host may serve it".
2. **A CI step that fetches it and verifies the digests**, failing on a mismatch
   rather than proceeding — a fixture that changed under us is a worse outcome
   than one that is absent. Cache it against the manifest's own hash.
3. **Somewhere for it to land that the suites already look in.** 94 suites define
   their own private `stories_dir()` = `CARGO_MANIFEST_DIR/../../stories`. Either
   the fetch populates that path, or the helper is consolidated first.

Confirmed 2026-08-25 that none of the free fixtures have moved: `advent.z6`,
`anchor.z8`, `scopa.z6`, `fmvpoker.z6` and all eleven `mysterious*.z6` are still
in `stories/` and `unit_tests/` holds none of them.

## Step 2 of the survey is unaffected and still owed

"Give the unguarded suites a non-vacuity guard, or one shared helper." Still
outstanding, and it gets MORE valuable under this strategy, not less: once CI
fetches some fixtures and not others, a suite that skips silently is no longer
telling you "CI has no media" — it may be telling you the fetch quietly failed.
94 private `stories_dir()` definitions is where that consolidation starts.
