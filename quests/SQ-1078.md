---
title: Select one story from a multi-story volume on the command line, not only through the story list
status: done
type: feature
priority: low
created: 2026-08-26T01:22:18Z
completed: 2026-08-26T04:47:02Z
commits:
  - ef83b1d9b696ac689de1ccf15dfa7877172718eb
  - 4918ff8b4022e9777e22917fb5873617a232a482
context: |-
  branch: main
  head: dc4039b1
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1063

  Raised while trying to reproduce SQ-1063's Macintosh Arthur flank defect: the reporter runs that press from InfocomMasterpieces.img via the story list, and no headless instrument can reach it because booting the volume opens whichever story it picks first.
tags:
  release: "0.3"
---

--- note 2026-08-26T01:22:36Z ---

## The report

> "i typically run mac arthur from the InfocomMasterpieces.img, which contains a
> bunch of games. you may want to add a command line arg to select individual
> stories from there (or you need to use the story list like I do)."

A volume with several stories on it can only be opened one way today: launch it and
pick from the story list. There is no way to name the story on the command line, so
**nothing headless can reach any story on such a volume except whichever one the
mount happens to return first.**

## Why it is worth doing beyond convenience

It cost a real diagnosis. SQ-1063 had a Macintosh Arthur flank defect that only the
reporter could see, because `flank_shape`, `hint_probe` and `pty_capture` all boot a
path and `stories/InfocomMasterpieces.img` boots **Zork Zero** (release 296 / serial
881019). The measurement was only possible at all because the reporter separately
supplied `stories/arthur.sit`, and even then it had to be unpacked by hand
(`unar`) to get at `STORY.DATA` and `PIC.DATA` — which is a different press from the
one the defect was reported on, so the two are not interchangeable evidence.

CLAUDE.md's own rule is that a finding must name the exact fixture and release, and
a volume we cannot address by story makes that impossible for every story on it but
one.

## Shape

Something the instruments can pass through, e.g.

```
lanthorn "stories/InfocomMasterpieces.img" --story ARTHUR
```

or the `volume:inner/path` spelling the auto-resume note already uses informally
(`InfocomMasterpieces.img:InfocomMasterpieces/ARTHUR`). Note that spelling is NOT
currently understood — `app::hints::load_mounted_story` returns
`NotFound` for it, which is worth checking before adopting it as the syntax.

Whatever it is, `pty_capture`, `hint_probe` and `flank_shape` should be able to pass
it straight through, since being unable to reach a press headlessly is the actual
cost.

## Related

An `--archive` boot already exists for picking a RENDITION
(`flank_shape --archive stories/zork0.cg1`), so there is a precedent for naming the
art; this is the same idea for naming the story.

--- note 2026-08-26T01:56:54Z ---

## And a caveat on the workaround, so it is not mistaken for a defect

Unpacking `stories/arthur.sit` with `unar` and booting its loose `STORY.DATA` +
`PIC.DATA` through `flank_shape --archive` DOES reach the art — that is how SQ-1063's
Macintosh Arthur flank defect was finally measured — but the press it reports is
wrong, and expectedly so:

```
booted as Amiga · release 54 serial 890606 · screen 480x300
```

Per the reporter: *"amiga and mac both share the same pic.data color format, so that
is expected since you were not booting directly from media (since it was a
directory)."* `InterpreterProfile::resolve` reads the MEDIUM, and a loose directory
is not one; `for_art_flavour` only sees a format the two machines share, so it cannot
tell them apart. Nothing is broken.

Two consequences worth carrying:

* A finding measured this way may name the wrong machine. It is sound for anything
  the ART decides — flank sections, picture geometry — and unsound for anything the
  PROFILE decides: palette, interpreter number, default colours, the v6 cell, the
  face. SQ-1063's sectioning is in the first group, which is why that measurement
  stands.
* It is a second reason for this quest. Unpacking an archive by hand gets the
  pictures but loses the press, so even the workaround cannot produce a capture that
  names its machine correctly — only addressing the story ON its volume can.

--- note 2026-08-26T02:12:25Z ---

## `zvm-cli` already has this — copy it, do not invent it

Reported: *"we already support this with zvm-cli."* Confirmed —
`crates/zvm-cli/src/main.rs` carries the whole thing:

* `cli_host::Opt::valued(&["--story"])` in its option table (line ~627);
* parsed into `story_pick: m.value("--story").map(str::to_string)` (~649);
* documented as *"`--story <n|name>`: which story to take off a multi-story disk
  image"* (~585), so it accepts an INDEX or a NAME;
* with a diagnostic already written for the failure this invites — *"a `--story` that
  matches nothing, or a choice nobody is there to make"* (~440) — and a note that the
  scripted `--story` path is the one where nothing else on screen would show which
  game was opened (~480).

So the work is to give `app` the same flag with the same spelling and the same
matching rule, not to design one. Two front-ends offering different spellings for one
idea would be its own defect.

## And the name must be the one the volume spells

Worth pinning here because it cost time on SQ-1068: the entry is the stored path
INCLUDING its directory, e.g.

```
InfocomMasterpieces/ARTHUR FOLDER/STORY.DATA
InfocomMasterpieces/ZORK ZERO/STORY.DATA
InfocomMasterpieces/PLANETFALL
```

Guessing `InfocomMasterpieces/ARTHUR` silently produced a basename-fallback save key
and looked exactly like a serious collection bug. `MountedDisk::stories()` returns the
`DiskStory::name` each format spells; a `--story` that takes an index or a substring
(as `zvm-cli`'s does) avoids making the player type any of that.

## Why it is worth doing for the 0.3 cycle

Without it, no headless instrument — `flank_shape`, `hint_probe`, `pty_capture` —
can reach any story on a multi-story volume except the format's tiebreak.
`stories/InfocomMasterpieces.img` opens **Zork Zero**, so the Macintosh Arthur press
in SQ-1063 could not be measured at all until the reporter separately supplied a
StuffIt archive of it, which had to be unpacked by hand and even then booted under
the wrong profile (see the note above).
