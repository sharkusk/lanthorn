---
title: 'gvm @pow / @dpow: special-case pow(1, y) = 1 and pow(x, ±0) = 1 even for NaN operands (Glulx spec float rules) before calling the platform powf, so glulxercise floatexp passes on Linux and Windows as it does on macOS'
status: partial
type: bug
priority: high
created: 2026-09-08T22:42:00Z
commits:
  - 0ff21f8dddc9998ae467abe9bf6c6f21fa5c2dfd
context: |-
  branch: main
  head: 553cb556
  cwd: /Volumes/Videos/Source/babelmap

  CI run 34283470859 on commit 553cb556: the widened glulxercise harness (SQ-1417) fails the floatexp group on ubuntu and windows with four sub-cases (pow(1,NaN), pow(1,-NaN), pow(NaN,0), pow(-NaN,0) return the NaN, "should be 1.00000 or $3F800000"); macOS passes because Apple's libm follows C99 Annex F and gvm's op is a bare f32::powf / f64::powf (exec.rs ~1561, ~1650). glulxe special-cases these before powf. Main is red until this lands.
---

--- note 2026-09-08T23:18:06Z ---

Approved plan (user, 2026-09-08): one Sonnet lane. (1) gvm exec.rs @pow (0x1AB, f32) and @dpow (0x21B, f64): before calling powf, return 1.0 when the base is exactly 1.0 (any exponent incl. NaN) or the exponent is ±0 (any base incl. NaN), per the Glulx spec's float section and glulxe's exec.c; platform-independent unit tests for the four failing sub-cases plus the ordinary cases. (2) Harness `crates/gvm-cli/tests/glulxercise.rs`: count BOTH result markers ("Passed." and "N tests failed.") so a failing group ends the wait immediately and the assertion names it; replace the single total deadline with a per-result inactivity timer (~60 s since the last result) that fails naming the group that stalled, keeping the 30 s x groups total as an outer safety net; also assert zero "tests failed." markers. Verify: gvm + gvm-cli nextest, glulxercise all groups locally, clippy; push, watch CI on all three platforms (the failing case was ubuntu/windows only).
