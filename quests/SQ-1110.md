---
title: Ship a generated player-word to IF-verb synonym table, so a rejected word can be bridged to one the story knows
status: done
type: feature
priority: low
created: 2026-08-28T04:26:02Z
completed: 2026-08-28T05:03:17Z
commits:
  - 021c8d7ee2087be63380b52c15b6e34a2109f487
context: |-
  branch: main
  head: b6f61631
  cwd: /Volumes/Videos/Source/babelmap

  Fell out of reviewing SQ-1041 mid-flight: guess-the-verb's motivating case (illuminate to light) cannot be solved by edit distance, stemming or grammar shape, because those all operate on form and the bridge required is meaning.
---

--- note 2026-08-28T04:26:24Z ---

## The problem this exists to solve, stated plainly

A player types `illuminate lamp`. The story wants `light`. To suggest `light`
you must know what `illuminate` MEANS — and nothing in the story file records
that.

Every mechanism available today operates on **form**, and all of them fail here:

| mechanism | on `illuminate` -> `light` |
|---|---|
| edit distance | distance 8 on a 10-letter word — ranks near last |
| stemming / morphology | `illuminat-` reaches nothing |
| grammar shape (`SyntaxLine::accepts`) | matches on noun COUNT and prepositions, not on which object — `illuminate lamp` narrows to "verbs with a one-noun line", i.e. most verbs |
| the story's own synonyms (`Verb.words`) | only groups words the story ALREADY knows; `illuminate` is not one of them |

So the bridge has to come from outside the story file. There is no third option,
and SQ-1041 cannot solve this case no matter how its ranking is written.

## The shape: invert the thesaurus, generate offline, ship the table

Do NOT ship a general English thesaurus and query it at runtime. Instead:

1. The IF verb vocabulary is **bounded** — a few thousand distinct verbs across
   the whole corpus, with ~100 standard actions doing most of the work. Harvest
   it: `Grammar::verb_words()` over every story in `stories/` and `unit_tests/`
   gives the real set, on all three engines.
2. Expand each of those verbs through WordNet (or Moby) **offline**.
3. Ship the resulting **reverse map**: player word -> IF verbs it could mean.

Anything outside IF's vocabulary never enters the table, because there is nothing
for it to map to. That buys the coverage of a full thesaurus at a fraction of the
size, with no runtime dependency, no network, and an artifact a human can read
and check — `illuminate -> light, burn` is either in it or it is not.

**Ship the generator with the table.** This is the persistence rule from
CLAUDE.md applied to a build artifact: nothing goes in without its regeneration
inputs, or the table becomes a blob nobody can reproduce when the corpus or the
source changes.

## The invariant that keeps it honest

Expand the player's word through the table, then **intersect with THIS story's
actual dictionary**, and display only what survives. The table proposes; the
story disposes. The player is never shown a word the parser would reject, so
this does not violate SQ-1041's rule that lanthorn offers the story's own
vocabulary and never rewrites what the player typed — the table only decides
WHICH of the story's words to surface.

## Licence — settle before any data lands in the tree

WordNet 3.0's licence is permissive and attribution-style and should sit fine
under lanthorn's BSD-3-Clause; Moby Thesaurus is public domain and is the
fallback if it does not. **Read the actual licence text rather than trusting
that summary**, and record the finding in the quest before committing any
derived data. Attribution, if required, goes wherever the third-party notices
live.

## Placement

`app`, or a small crate below it — NOT `zvm`/`gvm`/`scott`, which take zero
external dependencies and have no business knowing about English. `app` already
carries `regex`, `zip`, `serde`, `clap`.

## Relationship to the other quests

- **SQ-1041** ships first and without this: typos (edit distance genuinely works
  for `lanturn` -> `lantern`), morphology, and the story's own verb synonyms via
  `Verb.words`. Its ranking should have a seam this plugs into later.
- A browsable "what verbs does this story understand" is the fallback that needs
  no semantics at all and is filed separately — when nothing matches, showing the
  player the vocabulary beats a worse guess, and a human matches meaning far
  better than any heuristic we can ship.

--- note 2026-08-28T04:43:31Z ---

## Lookup cost, and the one ordering rule that keeps it free

**Search is not a performance concern, provided the two sets are searched
differently.** Recorded here because the wiring happens in a later quest and this
is where whoever does it will look.

| step | cost |
|---|---|
| exact lookup in the synonym table | O(1) hash hit — table size irrelevant |
| building the map (first use, lazy via `OnceLock`) | single-digit ms, once per session, often never |
| edit distance vs THIS STORY's dictionary (hundreds to a few thousand words) | well under a millisecond |
| edit distance vs the synonym table's keys (tens of thousands) | 10–30ms, and pointless |

**The rule: fuzzy against the small set, exact against the big one.**
Typo-match the player's word against the story's own dictionary; only ever do
**exact** lookups into the synonym table. Never scan the table's keys fuzzily.

That is not only faster, it is more precise. A near-miss against a word this
story genuinely knows (`lanturn` → `lantern`) is strong evidence. A near-miss
against a generic English table is noise, and would let a typo of a synonym of a
verb this story does not have produce a suggestion — two inferences deep, with
nothing anchoring either.

**Timing context:** the lookup fires between the player's command and the game's
reply, after the parser has already failed. The Z-machine turn that produced that
failure took longer than the whole lookup will. There is no budget pressure here,
so do not trade auditability for speed — see the storage note above, which
settles on a greppable TSV for exactly that reason.

--- note 2026-08-28T05:02:19Z ---

## Licence findings — read from the actual text, not a summary

Both sources are usable and compatible with lanthorn's BSD-3-Clause. Both require
a notice to travel with derived data. There was **no third-party notices file** in
the repository; `THIRD-PARTY-NOTICES.md` at the root is new, and carries both
notices in full.

### WordNet 3.0 — usable, attribution-style

From `WordNet-3.0/LICENSE` in `WordNet-3.0.tar.gz`
(sha256 `640db279c949a88f61f851dd54ebbb22d003f8b90b85267042ef85a3781d3a52`).
The operative sentences:

> Permission to use, copy, modify and distribute this software and database and
> its documentation for any purpose and without fee or royalty is hereby
> granted, provided that you agree to comply with the following copyright notice
> and statements, including the disclaimer, and that the same appear on ALL
> copies of the software, database and documentation, **including modifications
> that you make** for internal use or for distribution.
>
> WordNet 3.0 Copyright 2006 by Princeton University. All rights reserved.

and

> The name of Princeton University or Princeton may not be used in advertising or
> publicity pertaining to distribution of the software and/or database.

What must be reproduced: the copyright line, the permission paragraph, and the
ALL-CAPS warranty disclaimer, verbatim, on every copy of anything derived —
which the generated table is. Naming WordNet as a source (README, table header,
notices file) is attribution and not "advertising or publicity", but lanthorn
must not use Princeton's name to promote itself.

Note the DB-only tarball `WNdb-3.0.tar.gz` carries neither the LICENSE nor
`verb.exc`; the generator wants the full `WordNet-3.0.tar.gz`.

### Frequency list — 12dicts 6.0.2, not a web-scale list

Moby Thesaurus II was not needed; WordNet's terms sit fine. The frequency list
the later brief asked for was the harder licence question, and the obvious
candidates are NOT usable:

* `first20hours/google-10000-english` — its own LICENSE.md says "Educational and
  personal/research use of this data is permitted under the LDC license… **I do
  not recommend using this data for commercial purposes without licensing it
  from the Linguistic Data Consortium.**" Rejected.
* `hermitdave/FrequencyWords` — README: "MIT License for code. **CC-by-sa-4.0
  for content.**" Share-alike on a derived database; rejected.

Used instead: **12dicts 6.0.2**, `Lemmatized/2+2+3frq.txt` (Alan Beale), sha256
`64ac1d35acb66b550c7ebc56e080b62e0bad8f5984d72059dc2e05ac48780e52`. Beale's
ReadMe:

> The 12dicts lists were compiled by Alan Beale. I explicitly release them to the
> public domain, but request acknowledgment of their use. (Actually, the
> dependency of the 2of12inf list and the 2+2+3 lists on AGID prevents their
> release into the public domain. However, I do not impose any additional
> requirements on their use beyond those imposed by AGID and its sources, as
> described in agid.txt.)

AGID (`agid.txt`), which therefore governs:

> Copyright 2000 by Kevin Atkinson
>
> Permission to use, copy, modify, distribute and sell this database, the
> associated scripts, the output created form the scripts and its documentation
> for any purpose is hereby granted without fee, provided that the above
> copyright notice appears in all copies and that both that copyright notice and
> this permission notice appear in supporting documentation.

AGID's own inputs are the Moby POS database and Moby word lists (public domain,
Grady Ward), ENABLE2K (public domain) and WordNet 1.6 (same Princeton terms).
The chain is permissive all the way down. What must be reproduced: Atkinson's
copyright line plus his permission notice, in supporting documentation.

It also happens to be the right list technically: it is **lemmatised**, headword
at column 0 with inflections indented, so one file supplies both the frequency
ranking (21 bands) and a source-backed inflection map — which is what lets the
base-forms-only rule be enforced without ever writing a suffix rule.

--- note 2026-08-28T05:02:44Z ---

## What shipped, and the numbers behind it

`crates/verb-synonyms` (reader + `src/synonym_groups.tsv`, zero deps) and
`crates/verb-synonyms-gen` (harvester + generator, zero external deps). `app` was
not touched; wiring is SQ-1041's seam.

### The harvest — the number the whole design rests on

119 stories read (60 Z-machine, 22 Glulx, 37 Scott) out of 353 files in
`stories/` + `unit_tests/`. **9,902 distinct verb spellings: 2,860 single words
and 7,042 verb+preposition phrases.** 2,078 of them are words WordNet knows as
verbs, which is the working set the table is built against.

The 70 skips are all correct refusals, not failures: `*izm.z5`/`*inv.z5` are
Infocom InvisiClues hint files with no grammar, `.blb` files are resource-only
blorbs with no executable, `Grammar: Absent` is the menu-driven v6 titles
(Journey, Scopa, Sunburst), and disk images are not read at all because mounting
lives in `app` and every game on a floppy here also ships as a bare story file.

### Coverage audit (the quality metric, not the row count)

1,365 common English verbs — 12dicts bands 1–11 (2,807 headwords, the band
boundary nearest "the top 3000"), **lemmatised before dedup**, then reduced to
those WordNet knows as verbs.

* reached by plain synonymy: **968 (70.9%)**
* reached after the one-hop gap-fill: **1,213 (88.9%)**

The 152 that remain unreachable are overwhelmingly correct misses — nouns used
verbally with no IF counterpart (`email`, `radio`, `budget`, `photograph`,
`network`, `garden`, `season`, `truck`). No common verb that obviously SHOULD map
was found among them.

### Inflected IF verbs — measured, not assumed

**23 of 2,860 single-word spellings (0.8%)** are forms WordNet knows only as an
inflection. The conversational hypothesis is confirmed with a nuance: `did`,
`seen` and `was` cluster exactly where predicted, in Infocom's interrogation
parsers — Deadline, Witness, Suspect and Ballyhoo. The rest are participles used
as commands (Midnight Swordfight: `broken`, `cooked`, `dancing`, `haunting`,
`petrified`, `roasted`, `waking`), social forms (`thanks`, `cheers`, `hooray`)
and one spelling variant (`disc`). `thanks`, `did` and `seen` are all words a
player plausibly types, so the imperative assumption holds as a strong tendency
rather than a law — worth stating as a caveat.

A further **9** spellings look inflected and are lemmas in their own right —
`saw`, `lay`, `rent`, `wound`, `fell`, `bound` — and are left exactly as the
story spells them. You saw a log, you lay a rug, you rent a room; lemmatising
those would have deleted six real IF verbs.

9 of the 23 were dropped as redundant (every story that accepts them also accepts
the base). 14 were kept, because some story has no base form and the game's
dictionary is ground truth.

### The table

2,758 groups, 9,735 memberships, mean group size 3.53, **74 KB** as sorted TSV.
Line order is significant (per-word WordNet sense order, via a topological sort
of all per-word constraints; 1,329 constraints could not be satisfied because two
words rank two shared senses in opposite orders, and those breaks are counted).

### The one judgement worth arguing with later

The gap-fill unions a synset with its immediate hypernym **only where the synset
itself contains no IF verb**, and that condition is load-bearing rather than a
coverage heuristic. A group is symmetric; hypernymy is not. Relaxing it to "any
synset" was measured — 92.0% coverage instead of 88.9%, 131 KB instead of 74 KB,
and it fixed `inspect → examine`, which WordNet does not give as synonymy — but
it also put `fish`, `hook` and `net` in a group with `grab`, every one of them a
wrong suggestion waiting for a game that implements it. Precision was chosen.
`inspect → examine` is a known, deliberate miss.
