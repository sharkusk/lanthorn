---
title: 'Kitty shm writer: reserve the object''s pages with posix_fallocate before mapping (Linux), so a full /dev/shm answers ENOSPC and takes the zlib fallback instead of SIGBUS — lanthorn''s kitty_shm_write and the ratatui-image probe PR alike; document --ipc=host for a local docker run'
status: open
type: bug
priority: low
created: 2026-09-07T05:12:32Z
commits: []
context: |-
  branch: main
  head: 8312fe31
  cwd: /Volumes/Videos/Source/babelmap

  User review of SQ-1374's unsafe code, 2026-09-07: ftruncate reserves nothing on Linux tmpfs, pages fault in on first touch, and a full /dev/shm (Docker's default is 64 MB, and "docker can also be used locally") kills the process with SIGBUS rather than returning an error. macOS is unaffected. Fix in both trees: `crates/app/src/render/graphics.rs` `kitty_shm_write` and the fork's `kitty-shm-probe` branch behind draft PR ratatui/ratatui-image#200 (and the `lanthorn-shm` pin branch).
---
