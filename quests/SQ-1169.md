---
title: A `parent` re-root in style.toml is a silent no-op on any row whose registry Delta pins an fg/bg
status: partial
type: bug
priority: low
created: 2026-08-31T17:10:32Z
commits:
  - a9898db9117395d0557f599541434ec25554f503
context: |-
  branch: main
  head: 488df93b
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1113

  Found while making tooltips default to the menu highlight (user's request, 2026-08-31): the user had already written the correct `[tooltip] background = { parent = "dialog.list_selected" }` in their own style.toml and reported it "didn't have any effect" — the row's pinned warm-dark pair was masking the inherited colours.
---

--- note 2026-08-31T17:10:53Z ---

## The mechanism

`resolve_row` (`crates/app/src/theme/resolve.rs:419`) composes in two steps:

1. the row's **registry default Delta**, applied on top of the resolved parent;
2. each user layer's decl (global → garglk → per-game), highest wins.

A user decl that sets only `parent` re-roots the row correctly — `effective_parent`
honours it — but carries no colour channels of its own. So step 1's pinned `fg`/`bg`
overwrite whatever the new parent supplied, and step 2 has nothing to put back. The
re-root resolves, emits no warning, and changes nothing on screen.

## The eight rows it applies to

Found by a paren-balanced scan of `REGISTRY` for `fg:`/`bg: Some(Color::` inside a
row's default Delta (181 rows scanned):

    status_header
    debug.disasm_executed
    debug.disasm_rd
    debug.disasm_soft
    dialog.shadow
    tooltip.background      ← FIXED, see below
    dialog.list_selected
    transcript_search_highlight

`tooltip.background` was cured incidentally by making it inherit
`dialog.list_selected` instead of pinning a literal pair. **The other seven are still
live**, and `dialog.list_selected` is the one that stings: it is the shared
Black-on-Cyan menu highlight, so a user who wants their menus retuned cannot re-root
it — and now the tooltip inherits from it, so neither moves.

## Why the existing test could never catch it

`user_parent_reroot_override_resolves` (resolve.rs) proves re-rooting works using
**`panel.title`**, whose registry Delta is `Delta::EMPTY`. A row with nothing to pin
cannot demonstrate the pin winning. That is the whole coverage gap in one line: the
mechanism test was written against the case that cannot fail.

`a_reroot_moves_the_colours_of_a_row_that_used_to_pin_them` now covers the shape, but
only for `tooltip.background`.

## The design question, which is NOT settled and is the user's call

What should `parent = "x"` mean on a row whose registry Delta pins colours?

- **(a) A re-root suppresses the registry Delta's colour channels.** Semantically the
  honest reading — "inherit from x" should mean the colours come from x. Risk: it
  silently changes what seven existing rows do for anyone who has already re-rooted
  one (probably nobody, pre-release).
- **(b) Leave resolution alone; stop pinning colours in the registry.** Each of the
  seven gets a parent that already carries the pair, as `tooltip.background` now
  does. More faithful to the existing model, but some of these have no honest
  parent — `transcript_search_highlight`'s Black-on-Yellow is not any role.
- **(c) Warn.** Detect a re-root of a colour-pinning row and emit a `ThemeWarning`,
  the way an unknown parent already does. Cheap, honest, fixes nothing.

(a) and (c) compose. Do not pick one from this note — ask.

## Related

Adjacent but separate: `dialog.list_selected` pins a literal `Color::Cyan` while the
`accent` role resolves through **slot 6** of the active scheme. So a custom scheme
retunes accent everywhere and leaves the menu highlight — and now the tooltip —
stubbornly literal-cyan. Worth deciding alongside, since (b) would force the question.

--- note 2026-08-31T17:42:47Z ---

## Status correction (2026-08-31)

Commit `a9898db9` carried a `Confirm:` trailer, which advanced this to `confirm`. That
was wrong and it has been set back to `partial`.

What that commit actually did: cured **`tooltip.background`** by making it inherit
`dialog.list_selected` instead of pinning a literal pair, and added
`a_reroot_moves_the_colours_of_a_row_that_used_to_pin_them` as the regression for that
one row. The user's reported symptom is fixed.

**What it did NOT do, and what keeps this open:** the seven other colour-pinning rows
still swallow a `parent` re-root, and the design question above — (a) suppress the
Delta's colours on re-root, (b) stop pinning in the registry, (c) warn — is still the
user's to settle. Nothing here should be closed until that decision exists and the
seven rows follow it.

--- note 2026-08-31T18:03:21Z ---

## Measured by SQ-1170's sweep: only TWO of the seven are total no-ops

`every_key_the_template_documents_takes_effect_when_uncommented`
(`crates/app/src/theme/template.rs`) now re-roots every documented row onto each
of the seven roles in turn and requires at least one to move a COLOUR. Run
against the seven rows listed above, only two fail:

    dialog.list_selected          fg Black + bg Cyan  — both channels pinned
    transcript_search_highlight   fg Black + bg Yellow — both channels pinned

The other five — `status_header`, `debug.disasm_executed`, `debug.disasm_rd`,
`debug.disasm_soft`, `dialog.shadow` — pin ONE channel and inherit the other, so
a re-root still moves the unpinned one and clears that bar. They are **masked,
not inert**: `status_header` pins only `bg`, so `parent = "alert"` moves its ink
to yellow and leaves the black ground behind, which is arguably worse than doing
nothing — the user gets a half-applied re-root with no warning.

That distinction matters to the choice above. Option (c) (warn) has to fire on
all seven, not just the two, because a PARTIAL mask is the confusing case. Option
(a) makes all seven whole. Option (b) has to find an honest parent for all seven,
and `transcript_search_highlight`'s Black-on-Yellow still has none.

The two total no-ops are named in `PARENT_REROOT_UNPROVABLE` in that test file
with a pointer here. Nothing in the resolver was changed — the decision is still
the user's.
