---
title: Review zvm's public API surface for embedders outside lanthorn
status: done
type: feature
priority: low
created: 2026-08-23T01:10:12Z
completed: 2026-09-08T06:59:54Z
commits:
  - de299b2e341bf2ef1def773c830bccd40547c897
  - 9ef5e921843fc83a7d86d074b80152f5b1760b3e
  - 88cd5250a4f367d1b9cbd0b8746a8f2f073f52d6
  - ccf1ef205a9902cb651bf5970d2c65eca1f0e2a2
  - 43350c69364b3832f07e7515f04bac1a3bac28e9
  - bb05d39562691adb8390a9c5010413cb76932123
  - aa57b47e53c011ad3b3be691157b41e52b81b161
  - 9977376d57ea5f34f741e5ba92acc8e7a1dbcd3e
context: |-
  branch: main
  head: 9dade069
  cwd: /Volumes/Videos/Source/babelmap
  current: SQ-1003

  Raised alongside stating that zvm must become the standard Rust Z-machine implementation: its `pub` items are now a promise to strangers rather than to `app`, and nobody has yet read the surface as an outside consumer would.
---

--- note 2026-08-23T01:10:43Z ---

## Why now

`crates/zvm` is to be usable outside this project as the standard Rust Z-machine
implementation. That changes what `pub` means: it is a promise to strangers, not
a convenience for `app`. Nobody has yet read the surface as an outside consumer
would, and the crate grew under the assumption that `app` was its only caller.

## The three questions to ask of it

**1. What is `pub` that should not be?**
Things exposed because `app` happened to need them, which an embedder would
reasonably take for API and which would then be painful to change. `V6Cell`'s
methods, `write_screen_dims` / `write_screen_dims_px`, `Machine::set_v6_cell` /
`set_v6_screen_px` and the `ScreenState` internals are all recent and worth a
deliberate look — several were made `pub` this week for lanthorn's benefit.

**2. What is missing that an embedder needs?**
The known case is SQ-1013 (the Version 6 cell lives in `app`), but there will be
others. Useful test: could someone write a working v6 host against `zvm` alone,
without reading `crates/app`? Where they cannot, either the knowledge is
misplaced or the seam is missing.

**3. Do the docs read as written for a stranger?**
`zvm`'s comments are unusually good but frequently addressed to US — they cite
quest numbers, name lanthorn's own defects, and assume the reader knows what the
ring or the hybrid path is. A `SQ-0917` reference is meaningless to an embedder;
the ZMSD section number beside it is not. Prefer the spec citation first and the
quest as provenance.

## The seam question, which is the interesting one

The engine must not decide I/O, fonts or rendering — but it should DEFINE the
interfaces a host plugs into, because that is what makes it embeddable rather
than merely readable. Existing seams: `Output`, the picture/sound event queues,
`InterpreterProfile`-supplied colours.

The one SQ-0917 identified and did not build is `FontMetrics` — a host-supplied
answer to "how wide is this string, in the face I am actually painting". Under
the standard-implementation goal this stops being speculative: a GUI drawing
proportional text is the second consumer the goal anticipates. `V6Cell` already
documents the boundary it sits on (declared metric versus drawn), so the shape is
half-designed already.

**Do not build it as part of this review.** Decide whether it belongs, and file
it if so.

## Also worth deciding

- **Versioning and stability.** Pre-release today with no back-compat obligation
  (a standing project rule). At what point does `zvm` start owing semver to
  people who are not us?
- **Whether `gvm` and `scott` carry the same ambition.** The goal as stated names
  `zvm`; Glulx and Scott Adams may or may not follow, and the answer changes how
  much of this review generalises.
- **A doctest or example.** Every crate here sets `doctest = false`, which is free
  while nothing is public-facing and looks different for a crate meant to be
  embedded — a compiled example is the cheapest proof the API is usable.

## Related

- **SQ-1013** — the first concrete instance: the v6 cell is in the wrong crate.
- **SQ-0917** — where `V6Cell` and the declared-versus-drawn boundary came from.

--- note 2026-08-23T06:57:37Z ---

## Review complete (2026-08-23). Ranked, with file:line.

Zero-dep rule verified clean: `crates/zvm/Cargo.toml` has an empty `[dependencies]` and no dev-dependencies at all. ~480 pub items across 16 modules. Doc coverage is genuinely better than most published crates — the naming findings below are about AUDIENCE, not absence.

### 1. Process-global palette and interpreter version — the biggest embeddability defect (BREAKING)

`screen.rs:1787` `static ACTIVE_PALETTE`, `screen.rs:1793` `static INTERPRETER_VERSION`, with `set_palette`/`palette`/`set_interpreter_version`/`interpreter_version`.

The justification is in the doc itself (`screen.rs:1778`): "the palette is a property of the machine **lanthorn** is pretending to be, and there is exactly one of those per run." That is a lanthorn premise, not a Z-machine one, and it is false for any embedder wanting two sessions — a GUI with two windows, a web front-end serving N players, a parallel harness, an Amiga-vs-IBM comparison tool.

Three reasons this ranks first:

* **The project already pays for it.** CLAUDE.md records four consecutive red-main incidents (SQ-0904/0958/0959/0987) and a whole lock-discipline apparatus — a mutex private to `app`, a panicking setter, and the source-scanning `palette_lock_discipline` case — existing solely to make a process-global survive `cargo test`. An embedder inherits the hazard and none of the apparatus.
* **zvm's own docs argue against it.** `screen.rs:2222`, on `V6Cell`: "it is emphatically not process-global — see `zvm::screen::set_palette` for what that costs." The cell was already moved onto `Machine`. The palette is the same kind of fact.
* **The plumbing exists.** `true_colour_in(p, n)` (`screen.rs:1918`) already takes the palette as a parameter; only `standard_true_colour` (`:1904`) and `ZColour::true_value` (`:70`) read the globals.

The doc's counter-argument names "four unrelated call chains", two of which are HOST chains (the terminal cell renderer, the CLI SGR path) that should take the palette from the session they are rendering — which is exactly the seam split.

Action: `Machine::palette` and `Machine::interpreter_version`; thread into `true_value`, which already takes `interpreter_default`. Deleting the statics also deletes all of `app::v6_palette*`.

### 2. `pub mod fixtures` ships a test loader in the release library (BREAKING, trivial)

`lib.rs:4` → `fixtures.rs:11` does `PathBuf::from(env!("CARGO_MANIFEST_DIR"))`, unconditionally public, baking the BUILD MACHINE'S absolute source path into every downstream binary. No users outside zvm's own tests. Gate behind `cfg(test)` or a feature — `header::tests_support` (`header.rs:84`) is the in-crate precedent.

### 3. No boot recipe — the ordering constraints live in the host (additive)

An embedder gets `Machine::new` → a dozen independent `set_*` → `init_caps()` → drive to first input, with the order documented only in lanthorn: `app/src/session.rs:818` ("`set_picture_dims` runs right after `set_sound_available`, before `init_caps()`") and `:823` for default colours. `init_caps` (`exec.rs:601`) says it must be called and names nothing that must precede it. The result is an eleven-parameter host constructor (`session.rs:868`) reconstructed by reading lanthorn.

Action: `Machine::boot(mem, BootConfig { .. })` owning the ordering; keep the setters for mid-run changes.

**Note the overlap with SQ-1022**, which is building `app::MachineBoot` right now. They are different layers and both are wanted: `app::MachineBoot` answers "what does this MEDIUM say the machine is" (the four-link std_window cascade + cell), `zvm::BootConfig` answers "in what order must a Machine be told things". SQ-1022 should leave room for the second.

### 4. No screen-state snapshot seam — every embedder re-hand-rolls it (additive)

Quetzal carries no screen state by design, so save-anywhere forces a host to serialise `ScreenState`/`V6Windows`/`ZWindow`/`V6Text`/`ZColour` itself. `app/src/archive.rs` is 2603 lines doing exactly that — `ScreenDto` (`:308`), `V6WindowOpsDto` (`:133`), `ZColourDto` (`:451`) — a hand-rolled mirror that must move in lockstep with zvm's types. zvm already hand-rolls a dependency-free binary format (`quetzal.rs`), so nothing blocks a versioned `Machine::screen_snapshot()`/`restore_screen_snapshot()`. Must stay backend- and terminal-neutral (native pixels, no cell coordinates), per CLAUDE.md.

### 5. `Machine`'s pub fields ARE the API, and the drain protocol is prose (partly breaking)

`exec.rs:212` says "Fields are `pub` so Tasks 11+ can attach I/O channels" — a retired planning scheme, ~40 pub fields as a result.

* Draining is `std::mem::take` on bare Vecs with no method: `pending_sounds` (`:273`), `pending_pictures` (`:282`), `pending_erase_fills` (`:312`), `diagnostics` (`:367`), `screen_trace` (`:402`), `exec_pcs` (`:407`), `v6_prose_retired` (`:328`). Miss one and it grows unboundedly.
* **The picture/fill interleave rule is correctness-critical and exists only as a comment.** `exec.rs:148` (`pics_before`) explains that draining the two queues separately replays a v6 turn in the wrong order and erases the artwork. Every embedder must reimplement that merge from prose. Should be `take_paint_events() -> Vec<PaintEvent>`, already merged.

### 6. zvm is doubling as lanthorn's shared-vocabulary crate (BREAKING)

* `ZColour::True24(u32)` (`screen.rs:53`) — doc says "used by the Glulx host". **No zvm code constructs it**; the only producers are `app/src/glk_backend.rs:75,788`. A Z-machine embedder writes a match arm for a structurally unreachable variant belonging to a VM this crate does not implement.
* `zvm::location::LocationMethod` is imported by `app/src/glulx_session.rs:27` — the Glulx adapter borrowing a zvm type as neutral vocabulary.

### 7. Missing seams

* **FontMetrics** — zvm names the gap itself (`screen.rs:~2210`): a proportional renderer "needs per-glyph advances, which is a `FontMetrics`-shaped thing supplied by the host". No such trait exists. `V6Cell` answers the DECLARED metric; there is no interface at all for the DRAWN one.
* **Resources are a pre-filled vec, not an interface.** `picture_dims: Vec<(u16,u16,u16)>` (`exec.rs:277`) forces a host to enumerate every picture before boot. A `trait Resources` would let a lazy/streaming host answer on demand. (Blorb living in a separate crate is right; the SEAM being a vec is the issue.)
* **`Output: Any` forces `'static`** (`io.rs:36`). A GUI sink borrowing a frame buffer cannot be used; hosts round-trip through `as_any_mut` downcasts.
* No RNG seam beyond `set_rng_seed` (`exec.rs:1038`). Minor.
* Save/restore file I/O is correctly host-side. No complaint.

### 8. More machine knowledge still in the host — same class as SQ-1013

Beyond `v6_font_cell` (`app/src/interpreter.rs:926`): `InterpreterProfile::std_window` (`:699`) with `MACINTOSH_STD_WINDOW` (`:1026`) and `AMIGA_STD_WINDOW` (`:1033`), plus `two_colour_colours` (`:765`), `row_number` (`:501`), `try_for_interpreter_number` (`:460`). `zvm::interpreter::MachineProfile` already carries number, default_colours, palette, period_look, v6_screen_page, one_screen_palette — std_window and the cell are the same kind of member. **Suggest folding both in one change rather than SQ-1013 alone**; `machines::table`'s exhaustive destructure forces the new columns to be printed, which is the built-in safety net.

### 9. Naming and docs

* **`lib.rs` has no crate-level `//!` docs at all.** docs.rs would show a bare module index with no statement of what zvm is, which versions it covers, or how to boot one. Cheapest high-value fix here.
* No `#![warn(missing_docs)]`, no lint attributes anywhere.
* **"lanthorn" appears in 18 published doc comments** across 5 files, plus types a reader cannot see (`GameSession::drain_turn` at `exec.rs:327,355`; "the `app` crate's `CaptureSink`" at `io.rs:46,61,64`) and a retired planning scheme ("Tasks 11+" `exec.rs:213`, "(Task 9)" `:279`, "(Plan 1b)" `:285`). None are wrong — all are load-bearing rationale — but each should name the ROLE with lanthorn as the worked example.
* `location.rs:1` is "Mapper-facing API" and `lib.rs:18` promotes six of its items to the crate root — the only such promotion — making `zvm::current_location` look like a VM primitive when it is a documented best-effort heuristic (`location.rs:19-21`). Keep the module; drop the root re-export.
* `machines.rs:1` opens by describing a CLI flag no embedder has.
* `screen.rs:1013` re-exports `AMIGA_INTERPRETER_NUMBER` mid-file, giving one constant two public paths for no stated reason.
* `amiga_global_colour_pair` (`:986`) / `machine_screen_pair` (`:1082`) / `amiga_screen_pair` (`:1038`) — three near-identical names for two different concepts.
* Undocumented pub items: `screen.rs:127,193,248,253,2154,2165,2449`; `dictionary.rs:14`; `objects.rs:132-138,460`; `exec.rs:807`; `state.rs:37`.

### 10. Stability hazards

* **Zero `#[non_exhaustive]` in the whole crate.** Enums with demonstrated or certain growth: `Palette` (`screen.rs:1626` — grew 2→5 across SQ-0719/0956), `ZColour` (`:49`), `StepResult` (`exec.rs:153`), `ZError` (`error.rs:5`), `LocationMethod`, `MachineLook`, `CursorShape`, `StatusBand`, `Provenance`/`Unit`/`CacheFmt`, `OpRole`, `Form`/`OperandCount`/`Operand`. Breaking now, free forever after — and pre-release is the moment.
* Host-READ-ONLY structs that should be non-exhaustive: `SoundEvent`, `PictureEvent`, `EraseFill`, `MachineProfile`, `PeriodLook`, `StackTrace`/`TraceFrame`, `Header`, `Token`, `ObjectSnapshot`.
* **Do NOT** mark `TextAttrs` (`io.rs:14`) or `V6Text` (`screen.rs:369`) non-exhaustive — hosts CONSTRUCT both. Give them constructors first if they must grow.
* **Opcode internals public with zero external users** (grepped app, zvm-cli, mapper): `cpu::state::{read_var, write_var, peek_stack, poke_stack, call_routine, return_value}` (`state.rs:52-208`), `State`/`Frame` with every field public (`:10-33`, including `pc`, `frames`, `eval_stack`) — which pins the call-stack representation forever — plus `Machine::do_branch` (`exec.rs:3642`) and `print_text` (`:4100`). `do_store` (`:4056`) is used externally but only from two tests.
* `Machine::out: Box<dyn Output>` is a pub field (`exec.rs:218`), swappable mid-run with no invariant.

### Suggested order

| # | change | breaking |
|---|---|---|
| 1 | palette + interpreter version onto `Machine`, delete the statics | **yes** |
| 2 | gate `fixtures` | **yes** (trivial) |
| 3 | crate-level `//!` docs | no |
| 4 | `BootConfig` owning the `init_caps` ordering | no |
| 5 | `take_*` drains + merged `take_paint_events()` | no |
| 6 | `#[non_exhaustive]` sweep | **yes** |
| 7 | fold `std_window` + `v6_font_cell` into `MachineProfile` (SQ-1013) | no |
| 8 | screen-snapshot format in zvm | no |
| 9 | privatise opcode internals and queue fields | **yes** |
| 10 | doc pass: de-lanthorn, drop the root re-export, decide `True24` | partly |
| 11 | `FontMetrics`, `Resources`, revisit `Output: Any` | partly |

1, 2, 6 and 9 get materially more expensive after a release. 3, 4, 5 and 8 are what an embedder feels on day one.

--- note 2026-08-23T19:45:19Z ---

## Review written up: `docs/zvm-embedding-review.md` (2026-08-23, second pass)

Full prose review now lives at `docs/zvm-embedding-review.md`. This note is the condensed ranking so it survives outside the file. **No `.rs` file was touched.**

### Corrections to the 06:57 note

* **SQ-1013 has LANDED.** Finding #8 of the earlier note is stale: `app::InterpreterProfile::v6_font_cell` (`interpreter.rs:892`), `std_window` (`:699`) and `default_colours` (`:704`) are now one-line forwarders over `zvm::interpreter::MachineProfile`'s `v6_cell` / `v6_std_window` / `default_colours`, and `AMIGA_STD_WINDOW`/`MACINTOSH_STD_WINDOW` are `pub use zvm::interpreter::{…}` at `app/interpreter.rs:941`. Every machine constant left in `app::interpreter` is a re-export. What remains misplaced is PROCEDURAL, not tabular — see #5/#6/#7 below.
* **The palette apparatus is far bigger than "46 test sites":** `crates/app` holds **234** `v6_palette`/`v6_palette_at_boot` guard acquisitions and **44** `v6_set_palette` calls.
* **Non-test panic sites in zvm: six, in 41k lines** — and one of them is reachable from two bytes (new finding, below).

### Ranked

**1. Process-global palette + interpreter version (BREAKING, free now).** `screen.rs:1787`/`:1793`. Blast radius inside the crate is FOUR non-test readers: `screen.rs:1579` (`init_header_caps`, `$1F`), `:1734` (`two_colour_card_request`, called from `exec.rs:1453` where `&self` is in scope), `:1905` (`standard_true_colour`), and `ZColour::true_value` (`:70-72`, reached from `exec.rs:3096-3097`). One non-test reader outside: `app/src/colors.rs:65`. `true_colour_in(p, n)` (`:1918`) already takes the palette by value — its own doc says a borrow-and-hand-back "is atomic to nobody", which is the session argument too. Only awkward step is `ZColour::true_value` gaining a palette param beside `interpreter_default`. Deletes `app::v6_palette*`, the private mutex and both `palette_lock_discipline` cases.

**2. `#[non_exhaustive]`: ZERO in zvm, gvm AND scott (BREAKING, free now).** `Palette` grew 2→5 (SQ-0719/0956); `ZColour` gained `True24`. Do NOT mark `TextAttrs` (`io.rs:14`) or `V6Text` (`screen.rs:369`) — hosts construct both.

**3. Privatise opcode internals + queue fields (BREAKING, free now).** No external callers for `cpu::state::{read_var,write_var,peek_stack,poke_stack,call_routine,return_value}` (`state.rs:52-208`); `State`/`Frame` expose `pc`/`frames`/`eval_stack`, pinning the call-stack representation as API. 27 of `Machine`'s 41 fields are pub, behind the retired "Tasks 11+" comment (`exec.rs:213`). `Machine::out` is a pub `Box<dyn Output>`.

**4. `pub mod fixtures` (BREAKING, trivial).** `fixtures.rs:11` bakes `env!("CARGO_MANIFEST_DIR")` — the BUILD MACHINE'S source path — into every downstream binary. `header::tests_support` (`header.rs:84`) is the in-crate gating precedent.

**5. NEW — two bytes panic a public function.** `disasm.rs:313` is a bare `unreachable!()` on `Operand::Var` under `OpRole::JumpOffset`. `operand_role` (`:121`) maps 1OP `0x0C` (`jump`) to `JumpOffset` regardless of encoding, and `decode.rs:276` decodes short-form type `0b10` into `Operand::Var`. So `0xAC 0x10` decodes cleanly and panics. `disassemble` (`:381`) exists to be pointed at arbitrary addresses. Trivial fix; render `jump (sp)`.
Otherwise the panic story is GOOD and worth saying: `Memory` latches OOB as `mem_fault` (`memory.rs:21`, `:162`) rather than indexing; `read_var` guards stack underflow (`state.rs:56-60`); the other five sites are infallible or `cfg(debug_assertions)`. **Running an untrusted `.z5` is safe today; disassembling one is not.** `cpu::disasm` + `cpu::disasm_cache` (2,844 lines, both pub in the release lib) are held to a lower standard than the interpreter — raise or feature-gate.

**6. No boot recipe → the host IS the spec (additive).** Three undocumented classes of setter (immediate / latched-at-`init_caps` / must-precede-the-boot-RUN); `init_caps`'s doc (`exec.rs:595`) names nothing that must precede it; the ordering lives in `app/src/session.rs:818`/`:823`. **Proof it costs:** the workspace's two embedders DISAGREE on `StepResult::Restart` — `session.rs:3762` calls `machine.restart()` (§6.1.3 reboot-in-place, Flags 2 preserved), `zvm-cli/src/main.rs:1739` rebuilds from original bytes and does not. Want `Machine::boot(mem, BootConfig)`. Note `set_interpreter_version`'s doc justifies the global by citing `GameSession`'s constructor — an `app` type — as a Z-machine constraint; `BootConfig` dissolves that.

**7. Drain protocol is prose (additive).** Seven queues drained only by `std::mem::take`, nothing enumerates them. The picture/fill interleave rule is correctness-critical and exists ONLY as a comment on `exec.rs:148` (`pics_before`) — drain the two separately and scopa's boot fill lands after the pictures and erases them. Want `take_paint_events()`, pre-merged.

**8. No screen-snapshot seam (additive).** `app/src/archive.rs` is 2,603 lines, largely a hand-rolled cross-crate mirror of zvm's screen types (`ScreenDto` `:308`, `ZWindowDto` `:490`, `V6WindowsDto` `:602`, `V6TextDto` `:484`, `GridCellDto` `:481`, `ZColourDto` `:456`, `V6WindowOpsDto` `:133`) that must move in lockstep with nothing checking. `quetzal.rs` proves zero-dep binary formats are fine here.

**9. Docs: excellent and largely INVISIBLE (non-breaking, an afternoon, highest value/effort).**
* **15 module headers use `//` not `//!`** — `screen`, `cpu/exec`, `memory`, `io`, `quetzal`, `location`, `objects`, `dictionary`, `header`, `error`, `cpu/decode`, `cpu/state`, all of `text/`. **137 lines of module orientation prose that docs.rs never renders**, including the two largest modules. Ten other modules already use `//!` — inconsistency, not policy.
* **`lib.rs` has no crate-level `//!` at all** — docs.rs shows a bare 16-module index.
* **`Machine::new`'s doc says "v6 is not supported"** (`exec.rs:521-523`). It means "v6 doesn't start from `initial_pc`". It READS as "zvm can't run v6", on the crate's most-read constructor, about its headline feature. `zvm-cli` is careful to disclaim this (`main.rs:519`); the library is not.
* `location` is the ONLY module promoted to the crate root (`lib.rs:18`) and its 45-line "this is a best-effort heuristic reading global 0" caveat (`location.rs:8-22`) is one of the `//` blocks rustdoc discards. Drop the promotion.
* 15 rendered doc comments name lanthorn; several name types the reader cannot see (`GameSession::drain_turn` `exec.rs:327`,`:355`; "the `app` crate's `CaptureSink`" `io.rs:46`,`:61`,`:64`); three name a retired plan ("Tasks 11+" `:213`, "(Task 9)" `:279`, "(Plan 1b)" `:285`). `machines.rs:1` opens on a CLI flag. Lead with the ZMSD cite, keep the quest as provenance.
* `cargo doc -p zvm` emits 21 warnings (6× unresolved `MORE`, 6× public docs linking to private items).
* **Yes, add an example** and drop `doctest = false` when one exists — a compiled example is the cheapest proof the API is usable.

**10. Missing seams.** `FontMetrics` — zvm names the gap itself (`screen.rs:2206-2209`) and `V6Cell` has half-designed it; file it, don't build it here. **The unit-space picture rule is not in zvm at all**: `set_picture_dims`'s doc (`exec.rs:1018`) never says a v6 table must be reported art-native × art_scale; that lives at `app/src/session.rs:194` (`V6_ART_SCALE`) and zvm mentions art scale once, at `interpreter.rs:785`. An embedder doing the obvious thing hands the game half-size pictures. `picture_dims` as a pre-filled `Vec` should be a `trait Resources`. `Output: Any` (`io.rs:36`) forces `'static` for a test-downcast convenience.

**11. zvm as shared-vocabulary crate.** `ZColour::True24` (`screen.rs:53`) is documented as "used by the Glulx host"; **no zvm code constructs it** — producers are `app/src/glk_backend.rs:75` and `:788` only. `app/src/glulx_session.rs:27` imports `zvm::location::LocationMethod` and builds `zvm::ObjectSnapshot` (`:721`,`:1032`) and `zvm::screen::ZColour` (`:1044`).

### gvm / scott (brief)

**Neither has ANY process-global mutable state** — the palette problem is zvm's alone, which kills the "inherent to a VM core" defence.

**gvm has already built the seam zvm lacks:** `pub trait GlkBackend` (`glk.rs:540`), 34 methods of which only `as_any`/`as_any_mut` are required; `Machine::with_glk` (`exec.rs:762`) is the only constructor; explicit `StepResult`/`supply_*`/`deliver_*` protocol; pull-based timers (`exec.rs:5461`). `app`'s `AppGlk` implements it rather than defining it. **This is the model for zvm.** gvm's own costs: 99 KB disassembler pub in the release lib; `as_any` boilerplate exists so OUR suites can downcast to `TestBackend` (`glk.rs:539` says so); runtime faults escape only via a pub `diagnostics: Vec<String>` + `StepResult::Quit`, so an embedder can't tell a clean quit from a crash; **18** non-test unwrap/expect in `glk.rs` (`:1873`–`:2779`) that abort the host, contradicting the crate's own stated fault-and-continue policy (`error.rs:3-5`); `set_borderless` (`exec.rs:2880`) is terminal chrome inside a VM.

**scott is the tidiest and least documented:** all ~30 `Vm` fields `pub(crate)` with accessors, **zero** non-test panics. But `lib.rs` is 8 lines with no `//!`, two of four re-exports are GLOBS (`pub use database::*`, `pub use decompile::*`) so the surface is accidental — four unnamespaced constants at the crate root (`database.rs:48-51`); `pub use vm::Input` is DEAD (nothing constructs it; input arrives via `supply_line`); `restore` returns `Result<(), ()>` (`vm.rs:259`); `Vm::room_block()` (`vm.rs:952`) returns a pre-formatted display block whose doc describes lanthorn's top panel.

### Ledger

| # | change | cost | breaking | free now, expensive later |
|---|---|---|---|---|
| 1 | palette + interp version onto `Machine` | medium (4 in-crate, ~278 app) | **yes** | **YES** |
| 2 | `#[non_exhaustive]` sweep | small | **yes** | **YES** |
| 3 | privatise opcode internals + queue fields | small | **yes** | **YES** |
| 4 | gate `fixtures` | trivial | **yes** | **YES** |
| 5 | fix `disasm.rs:313` | trivial | no | — |
| 6 | `BootConfig` + one `Restart` answer | medium | no | — |
| 7 | `take_*` + merged `take_paint_events()` | small | no | — |
| 8 | screen snapshot in zvm | medium | no | — |
| 9 | `//`→`//!`, crate docs, an example | trivial | no | — |
| 10 | doc pass; drop `location` root re-export; decide `True24` | small | partly | partly |
| 11 | `FontMetrics`, `Resources`, `Output: Any` | large | partly | partly |

**1–4 are the whole point of doing this pre-release.** 5–9 are what an embedder feels on day one; 5 is one line and 9 is an afternoon.

### Open / undetermined

* Whether gvm and scott carry the same "standard implementation" ambition — not decidable from the code; a product call. Findings 2 (non_exhaustive) and 9 (crate docs) apply to all three regardless.
* When zvm starts owing semver to strangers. Suggest: cut 1–4 first, then publish, then owe.
* Whether `FontMetrics` belongs — leaning yes (it is the second-consumer case the goal names), but it needs its own quest and a GUI to design against.

--- note 2026-08-23T19:57:09Z ---

## CORRECTION + real panic audit (supersedes finding #5 of the previous note)

The previous note's finding #5 was **WRONG and is retracted**. A dedicated fuzzing pass (throwaway crate in scratchpad depending on zvm by path; `git status --porcelain -- crates/zvm` clean throughout) settled it properly. `docs/zvm-embedding-review.md` §6 and the ledger have been rewritten accordingly.

### Retraction

**`disasm.rs:313`'s `unreachable!()` is DEAD CODE, not a two-byte panic.** `Operand::Var` returns early eleven lines above, at `disasm.rs:295-301`, so the `JumpOffset` arm never sees a `Var`. My `0xAC 0x10` reproducer does not panic. All six non-test `unwrap`/`expect`/`unreachable!` sites in zvm are genuinely unreachable or `cfg(debug_assertions)`.

### What IS reachable — the mechanism is ARITHMETIC, not indexing

**`ZWindow::put_prop` (`screen.rs:667`) is the hole.** The v6 `put_wind_prop` setter writes the story's raw `u16` into every window field with **no clamp**: `1 => x_coord`, `5 => x_cursor`, `6 => left_margin`, `7 => right_margin`. Those fields are then combined with plain `+`:

| site | expression |
|---|---|
| `exec.rs:4256` | `w.x_coord.max(1) + w.x_cursor.max(1) - 1`, then `abs_x + fw - 1` |
| `exec.rs:4203-4204` | `w.y_cursor += fh` and `w.x_cursor = w.left_margin + 1` |
| `exec.rs:2776` | `self.screen.cursor_row = start_row + row` (`print_table`) |

Siblings at `exec.rs:3273`, `:3437`, `:4238`, `:4283`.

Reproduced — a v6 `main` of four instructions: `put_wind_prop w1 prop1=0xFFFF`, `put_wind_prop w1 prop5=0x7FFF`, `set_window 1`, `print_char 'A'` → `panicked at exec.rs:4256: attempt to add with overflow`. Property 6 instead → `:4204`. `exec.rs:2776` was found by blind fuzzing (3 of 12,000 random stories, debug; 0 of 12,000 release).

**All debug-only.** Release wraps silently. That is exactly the debug/release split CLAUDE.md warns about, and a library cannot choose its embedder's profile.

**The discipline already exists one file away, which makes this a "guard beats a convention" case verbatim:** `screen.rs:529` writes the IDENTICAL expression as `self.x_cursor = self.left_margin.saturating_add(1)`; `exec.rs:3995` writes `w.x_cursor.saturating_add(fw)`; `set_cursor` clamps (`exec.rs:2485-2486`). Only `put_wind_prop` doesn't. **Clamping inside `put_prop` — or privatising the four fields behind saturating accessors — fixes all six sites at once.**

### Three more

* **`V6Cell`'s guard is defeated by a struct literal.** `V6Cell::new` (`screen.rs:2237`) clamps each axis to ≥1 and its doc says why ("would otherwise panic somewhere far from the mistake"), but `w`/`h` are `pub` (`:2226-2227`), so `V6Cell { w: 0, h: 0 }` reaches the divisions at `exec.rs:4255` and `:3275`. **Division by zero panics in BOTH profiles.**
* **`DisasmCache` panics in RELEASE.** `code_region` (`disasm_cache.rs:551`) returns `(min(high_mem_base, boot_root), mem.len())` with no `start < end` check. A header `$04`/`$06` past EOF ⇒ `region_start > region_end` ⇒ `build`'s loop never runs ⇒ empty `units` ⇒ `unit_index_at` (`:251`) evaluates `self.units.len() - 1`. Debug: the `debug_assert!` fires. Release: the subtraction wraps and `next_addr` indexes `units[0]` on an empty vec. `DisasmCache::empty()` (`:96`) builds the same object by design. `app/src/session.rs:4102` navigates this cache.
* **One legal instruction is an unbounded DoS.** `print_table` (`exec.rs:2759-2778`) loops `height × width`, both story-controlled `u16`, uncapped. Measured in **release**: one nine-byte instruction ran **29.2 s** and peaked at **4.3 GB** RSS before faulting. The host cannot interrupt — `step()` IS the interruption point. `copy_table` same shape. `State::frames`/`eval_stack` (`state.rs:29-31`) have no depth cap, so runaway recursion is an OOM abort not a fault. The awareness exists: the v6 grid IS capped (`GRID_CELL_CAP`, `screen.rs:2323`, applied `exec.rs:2065`/`:3275`).

### What's genuinely strong (worth recording so nobody "hardens" it twice)

* **`memory.rs` has NO raw indexing of a story address.** `read_byte`/`read_word` (`:95`,`:124`) use `.get()`, return 0, latch `mem_fault`; `write_byte`/`write_word` (`:115`,`:141`) refuse writes ≥ `static_mem_base`, latch, leave memory untouched. Drained by `step()` at `exec.rs:1109-1120` → `StepResult::Fault`. The in-file test is named `oob_read_latches_fault_instead_of_panicking` and its comment says "Previously panicked (unchecked slice index)".
* **Dispatch is total** — catch-alls at `exec.rs:1509`/`:1637`/`:1767`/`:2853`/`:3584`; `decode_form` covers all 256 opcode bytes. All-`0xFF` story faults at step 61; all-`0x00` at step 1281; 12,000 random stories never wedged it.
* **`Machine::new` is clean on every malformed input** — 10-byte ⇒ `NotAStoryFile`; v1/v2/v9 ⇒ `UnsupportedVersion`; `static_mem_base` past EOF ⇒ `Truncated`. (Load-bearing coupling worth a comment: `with_output` does `mem.raw_bytes()[..static_mem_base]` at `exec.rs:531-532`, in-bounds only because `Memory::new` rejected it at `memory.rs:77-79`.)
* **0 panics** from 10,000 random stories through `dictionary`/`objects`/`text::decode`/`ifid`/`world`/`location`/`save_quetzal`, and from 2,000 hostile Quetzal buffers through `restore_quetzal`. Abbreviation expansion (`text/decode.rs:134-155`) is depth-capped, so the classic nested-abbreviation stack overflow is absent.
* Only **11 of 29.5k lines'** worth of functions return `Result`, 6 public — and that is CORRECT design, since the running-story channel is the fault latches.

### Bottom line

**Debug-built host: zvm is NOT safe to point at an untrusted `.z5`.** **Release-built: closer, but no** — `DisasmCache` still panics and one `step()` can burn 29 s / 4.3 GB uninterruptibly. The core (memory, decode, dispatch, quetzal) is genuinely well hardened; the damage is concentrated in the **v6 screen model** and **two helper APIs**.

No `fuzz/` dir anywhere in the workspace and no malformed-input corpus (zvm has zero deps by policy). The one adversarial test, `crates/zvm/tests/object_scan_eof.rs`, exists because `czech.z5` once panicked in `read_byte` — this bug class has bitten before and was patched at the symptom. **An in-repo `#[cfg(test)]` random-story harness costs no dependency and would have caught the `print_table` and `DisasmCache` bugs in seconds.**

### Ledger, revised (robustness promoted to #2)

| # | change | cost | breaking | free now, expensive later |
|---|---|---|---|---|
| 1 | palette + interp version onto `Machine` | medium (4 in-crate, ~278 app) | **yes** | **YES** |
| 2 | **clamp `put_prop`; cap `print_table`/`copy_table`; `unit_index_at` on empty; privatise `V6Cell` fields** | small — 6 overflow sites collapse to one clamp | partly | partly |
| 3 | `#[non_exhaustive]` sweep | small | **yes** | **YES** |
| 4 | privatise opcode internals + queue fields | small | **yes** | **YES** |
| 5 | gate `fixtures` | trivial | **yes** | **YES** |
| 6 | `BootConfig` + one `Restart` answer | medium | no | — |
| 7 | `take_*` + merged `take_paint_events()` | small | no | — |
| 8 | screen snapshot in zvm | medium | no | — |
| 9 | `//`→`//!`, crate docs, an example | trivial | no | — |
| 10 | doc pass; drop `location` root re-export; decide `True24` | small | partly | partly |
| 11 | `FontMetrics`, `Resources`, `Output: Any` | large | partly | partly |

**#2 should be done regardless of any release schedule** — until it lands no host can point zvm at a story it did not write. 1/3/4/5 are the pre-release ones. Also worth filing separately: **gvm has 18 non-test `unwrap`/`expect` in `glk.rs` (`:1873`–`:2779`) that abort the host**, contradicting its own stated fault-and-continue policy — a worse absolute count than zvm's, in the crate whose seam design zvm should otherwise copy.

--- note 2026-09-04T23:59:25Z ---

Consider making `lanthorn-grammar-model` an optional dependency of zvm (and gvm/scott for symmetry): `optional = true` behind a default-on `grammar` feature (`grammar = ["dep:grammar-model"]`), with `#[cfg(feature = "grammar")]` on the grammar reader module AND on the object-table functions/re-exports that return vocabulary types (`objects.rs` re-exports `ObjectWords`/`Adjectives`, so the gate cuts into core object code, not just `grammar.rs`). CLI clients would set `default-features = false`. Caveats noted 2026-09-04: cargo unifies features across packages built in one invocation, so release-workflow CLI binaries still get the feature unless built standalone; grammar-model is a small zero-dep types crate, so the size/compile win is negligible — the value is purely API presentation for external embedders who want a single-crate zvm. (User asked for this to be folded into the API review rather than filed separately.)

--- note 2026-09-07T21:13:06Z ---

## Plan committed 2026-09-07 (user: finish a couple of small tasks, cut a 0.5.x release, then tackle this)

### Status against the ledger, measured on main 777df224
LANDED: put_wind_prop clamp (WINDOW_PX_CAP, tested); V6Cell fields private behind the guarded constructor (7ca93826); DisasmCache::unit_index_at uses checked_sub; SQ-1013 (std_window + cell in zvm::interpreter::MachineProfile).
OPEN: (1) palette + interpreter version still process-global statics (screen.rs ~2257/2263) with app's whole lock apparatus; (2) non_exhaustive: zvm 3, gvm 4, scott 0; (3) opcode internals + Machine queue fields still pub; (4) `pub mod fixtures` still ships; (5) print_table/copy_table uncapped; (6) no BootConfig / one Restart answer; (7) no take_paint_events drain protocol; (8) crate-level docs: zvm 0 lines, scott 0, gvm has them; no example; doctest=false everywhere; (9) `location` root re-export, True24, lanthorn-addressed docs; (10) grammar-model optional feature; (11) screen snapshot, FontMetrics, Resources — design later. gvm: 19 non-test unwrap/expect in glk.rs. scott: no crate docs, two glob re-exports, dead `Input`.

### Wave plan (after the 0.5.x release)
Wave 1 (breaking, pre-release-cheap): Opus lane — palette + interpreter version onto Machine, delete the statics and app::v6_palette*/palette_lock_discipline. Sonnet lanes in parallel — non_exhaustive sweep across zvm/gvm/scott (never TextAttrs/V6Text); privatise opcode internals + queue fields + gate fixtures; print_table/copy_table caps + DisasmCache/gvm unwrap hardening (gvm glk.rs 19 sites → fault-and-continue).
Wave 2 (day-one ergonomics): crate //! docs + an example per crate and drop doctest=false where one exists; BootConfig owning the init_caps ordering + one Restart answer shared by app and zvm-cli; take_* drains + merged take_paint_events(); drop the location root re-export, decide True24, de-lanthorn doc comments; grammar-model behind a default-on `grammar` feature.
Wave 3 (design): screen snapshot in zvm; FontMetrics; Resources trait; Output: Any.
OPEN PRODUCT CALL (user): do gvm and scott get the full treatment or only what any published crate needs (non_exhaustive, crate docs, gvm unwraps)? Wave 1 is written for the second; the first roughly doubles wave 2.

--- note 2026-09-08T01:27:23Z ---

2026-09-07 user decision: gvm and scott get the FULL treatment, all three waves, because all three engines are meant to be standard Rust implementations usable outside lanthorn. Wave 1 dispatched as three lanes: SQ-1393 (palette + interpreter version onto Machine), SQ-1394 (non_exhaustive / privatise / fixtures / scott re-exports), SQ-1395 (caps + gvm faults). Next steps revisited after wave 1 lands.

--- note 2026-09-08T05:02:53Z ---

2026-09-08: wave 2 landed on main as SQ-1396 (BootConfig, one Restart answer, take_paint_events, gvm drains), SQ-1397 (event-struct constructors, location re-export dropped, True24 kept and re-documented, grammar feature, scott room accessors), SQ-1398 (gvm/scott crate docs + examples), SQ-1399 (zvm crate docs + example). User decisions: True24 stays as a general 24-bit host colour; Machine::boot configures only, does not step. Remaining: wave 3 (screen snapshot in zvm, FontMetrics, Resources trait, Output: Any, then constructors for the host-built screen structs). SQ-1400 filed for the Counterfeit Monkey boot-timing flake.

--- note 2026-09-08T18:19:21Z ---

Reference-quality audit of `crates/scott` vs ScottFree 1.14 (correctness/completeness, not API).
Oracle: ScottFree 1.14 (ifarchive ScottFree.tar.gz, ScottCurses.c) built with a stdout curses stub +
fixed srand, diffed against `cargo run -p lanthorn-scott --example run_story` over all 26 `.dat`
in stories/ + tiny_cave. Also read cspiegel/scottfree-glk scott.c/scott.h, Swansea "Definition",
angstsmurf/spatterlight terps/scott (scott_actions.c, scott_defines.h).

RANKED GAPS (games affected / effort)

1. Single-letter directions + `i` are not expanded. scott.c GetInput maps n/e/s/w/u/d/i to full
   words BEFORE vocab lookup; vm.rs run_turn (vm.rs:744-770) has no such step, so with wl=3 "n"
   never matches "NORTH". Measured: a 30-command script gives "You use word(s) I don't know!"
   17-26x in 26/26 games, 0-1x in ScottFree. 26 games. ~10 lines.
2. Opcode 65 never wins the game. scott.c case 65 prints "Well done." + goto doneit ("The game is
   now over." + exit) when n==Treasures; vm.rs:648 `65 => self.print_score()` (vm.rs:1156) only
   prints. Reproduced on scratch probe.dat: SF ends the game, scott loops forever. Op 65 is in
   14 files (adv01/02/06/08/09/13/14a, quest1/2, sampler1, BURGLAR, MAROONED, MINER, tiny_cave).
   ~10 lines.
3. Opcode 63 prints nothing. scott.c prints "The game is now over.\n"; vm.rs:653 `63 => quit=true`.
   137 uses across 27/27 files. 1 line.
4. Backtick is not converted to `"`. ScottFree ReadString (both 1.14 and glk) does `if(c=='`')
   c='"'`; loader.rs Lexer::next_str (loader.rs:62-80) does not. Live in 25/27 files (adv01 has 38);
   visible as: Welcome to Adventure number: 1 `ADVENTURELAND`. 1 line.
5. No death-in-the-dark. scott.c PerformActions prints "Dangerous to move in the dark! " then, on a
   missing exit, "I fell down and broke my neck. " + glk_exit; vm.rs:783-802 just says "I can't go
   in that direction." Hit in BURGLAR.DAT and adv14a.dat inside 30 commands. ~10 lines.
6. Lamp: no "Your light is growing dim." (LightTime<25 && %5==0) and run-out prints once, not twice
   (SF decrements to -1 then stops). vm.rs:895-901. Also prints run-out even when the lamp is
   neither carried nor present. 26 games. ~15 lines.
7. PerformActions' -2 reply "I can't do that yet. " is absent: vm.rs:891 collapses "verb/noun matched
   but conditions failed" into "I don't understand your command." quest1.dat: SF=3, RS=0 in 30 cmds.
   ~15 lines (track a matched-vocab flag).
8. GET/DROP ALL: no "Nothing taken."/"Nothing dropped." (vm.rs:929,947 say "I don't understand your
   command."), no dark check ("It is dark."), no recursive PerformActions per item, no skip of
   `*`-prefixed autoget. 13/7 games in the sweep. ~25 lines.
9. Wording drift throughout: "OK." vs "O.K. ", "You are carrying:" vs "I'm carrying:\n…-…", "You have
   N out of M treasures." vs "I've stored N treasures.  On a scale of 0 to 100, that rates R.",
   "You have died." vs "I am dead.", "I need a direction." vs "Give me a direction too.", "What?" vs
   "What ? ", exits joined ". " vs ", ", dark look "It is too dark to see." vs "I can't see. It is
   too dark!", op-78 counter "N\n" vs "N ", op-84/85 echo uppercased (SF echoes as typed). No
   -y/-s/-t/-p option family (YOUARE, SCOTTLIGHT, TRS80_STYLE, PREHISTORIC_LAMP). All 26 games.
10. CARRIED is -1 in the VM but 255 in the file: cond 17/18 (item at initial loc) compares item_loc
    (-1 after op 52/74) with start_loc (255). vm.rs:551 item_at_start. Reproduced on probe.dat.
    Latent on this corpus (no overlap) but systematic on C64/Spectrum-derived .dat, where the
    Definition says 255 is the norm. ~5 lines (normalise at load).
11. Auto-noun uses the LAST /…/ pair and requires a trailing '/'; ScottFree uses the FIRST '/' and
    tolerates a missing close, plus `//` and `/*` "no autoget" markers. loader.rs:84-93. Live on
    secret.dat item 33 "Luger/LUGER/GUN/" -> scott shows the item as "Luger/LUGER" and binds GUN;
    SF shows "Luger" and binds LUGER. Also `//` would yield Some("") and be swept up by GET ALL.
12. Lexer gaps vs ReadString: no `""` escaped-quote rule, no CR strip, no non-ASCII -> '?';
    Database::parse takes &str and ScottSession::new_with_trace (scott_session.rs:100) rejects any
    non-UTF-8 .dat outright. Latent here (0 real escapes, 0 CR inside strings) but a Latin-1 .dat
    is unloadable. Taking &[u8] is an API change.
13. Occurrence pass rolls AFTER evaluating conditions (vm.rs:997-1013); ScottFree rolls first
    (`vv==0 && RandomPercent(nv)`), so the PRNG stream differs. Cosmetic given the seed is ours.
14. No ScottFree save-file import (its textual 16x"counter room" + state line + item locations).
    Snapshot/restore is ours only. ~60 lines.
15. Format dialects: only the canonical ScottFree text format is read. TI-99/4A bytecode, C64/ZX/
    Atari/Apple snapshot extraction, Mysterious Adventures native, compressed action tables
    (Gremlins, Supergran, Robin of Sherwood, Seas of Blood) and SAGA graphics are all unsupported
    and refused with a generic BadInt/Truncated rather than a named dialect error. Gargoyle spends
    ~100 KB of C on this (detect_game.c + parser.c). Large.

NOT defects: opcodes 90..=101 are unused by all 27 files (scan of every action table); no
out-of-range flag/counter/room param anywhere; bounds checks are stricter than ScottFree's
(no /0 in op 65, no read past Messages[]). 185 hostile/truncated/mutated .dat inputs: 0 panics,
0 hangs, 56 loaded and played. Condition codes 0..19 are all implemented and all match ScottFree
(incl. cond 16 as `>` per scott.c and Gargoyle, not `>=` as the Swansea Definition says).

--- note 2026-09-08T18:24:49Z ---

## gvm reference-quality audit (2026-09-08) — condensed. Oracle: glulxe 0.6.1 -O2 + cheapglk 1.0.7, built from source; specs fetched, not recalled.

**Headline: opcode coverage is COMPLETE** — every Glulx 3.1.3 opcode 0x00–0x239 dispatches (exec.rs:1180–1652), including float, double, heap, ExtUndo; unknown → `Err`, no panic. Gestalt 0–13 all answered and TERP/GLULX versions are right (exec.rs:2344/2346/2351). All 13 accel functions + 9 accelparams match glulxe's accel.c exactly. All 123 Glk 0.7.5 selectors dispatch (zero missing). **glulxercise Release 13: gvm 63/68 groups, glulxe 68/68.**

### Ranked gaps (S = spec-required, G = glulxe-compatible behaviour beyond spec)

1. **S — `decompress_ram` rejects every foreign Quetzal save.** exec.rs:2749–2754 errors `BadSave("CMem data truncated")` when the RLE stream ends early; glulxe DELIBERATELY omits the trailing zero run (serial.c write_memstate) and its reader fills zeros. Proven live: `unit_tests/startsavetest.gblorb`'s shipped save decompresses to 7,794 of 17,152 needed bytes → gvm prints "we tried to load the autorestore file, but it didn't work", glulxe restores it. Counterfeit Monkey's resource-9998 boot save is short by 291,288 bytes for the same reason — SQ-0595's fast path is masked by the sidecar gvm writes itself. **Fix: fill `[addr, memsize)` from `orig_byte` on exhaustion.** Unblocks SQ-0229. exec.rs:6482 pins the writer invariant as if symmetric.
2. **S — `@restart` violates `@protect` and clears the undo chain.** exec.rs:2918 `reset_ram()` ignores the range; :2944 zeroes `self.protect`; :2945 clears `undo_stack`. glulxe's vm.c:186 skips protected bytes and its comment says "we do not reset the protection range"; `vm_restart` never touches the undo chain. This is exactly why glulxercise's **`undorestart` group produces no result on gvm** (it also derails a whole `all` run).
3. **S — fmod/dmodr/dmodq use `a - (a/b).trunc()*b`** (exec.rs:1508–1517, 1585–1596) instead of glulxe's `fmodf` + `(a-r)/b`. glulxercise **floatmod 15 failures, doublemod 11**: `mod(1,0)`/`mod(±Inf,1)` give quo ±Inf (spec: both NaN), `mod(x,±Inf)` gives rem NaN (spec: rem = x), `mod(-0,1)` loses the −0 sign; and the f32 quotient rounds before truncation, so large quotients are wrong by construction.
4. **S — `ftonumz`/`ftonumn`/`dtonumz`/`dtonumn` map −NaN to 0x7FFFFFFF** (exec.rs:1489–1500, 1555–1569); spec/glulxe: signbit ⇒ 0x80000000. glulxercise **floatconv 8, doubleconv 2**. Infinities are already right via Rust's saturating cast.
5. **S/design — call-stub DestTypes 10–14 do not exist.** Filter iosys and string-embedded routines run a NESTED native dispatch loop, `run_call_to_return` (exec.rs:3816–3833). Consequences: output silently DROPPED past `FILTER_MAX_DEPTH = 32` (exec.rs:3538–3543) with no fault; `print_object` capped at depth 256 (:3709); a `@throw` past the awaited frame leaves `while self.fp != resume_fp` looking for a frame pointer that has gone below it (op_throw sets fp directly, exec.rs:2149); and a `@save` taken inside a filter/string routine cannot serialise the native frame, so such a save is neither resumable nor glulxe-readable.
6. **S — `@save`/`@restore` only work on a fileref-backed stream.** exec.rs:1394/1427 route through `stream_saveload_info` (glk.rs:2560), which knows savegame + file streams only; a memory stream falls to `unwrap_or_default()` → nameless request. Spec §2.10: @save writes to whatever output stream it is handed. `stream_restore_bytes` (glk.rs:2578) covers resource streams only.
7. **S — `restoreundo` restores the protect RANGE from `GReg`** (exec.rs:2549); spec says the range is not part of saved state. Both existing tests hold the range constant and cannot see it.
8. **S — seven hostile-reachable aborts, none an `unwrap`** (dev profile aborts; release silently wraps to a wrong address): `@protect` unvalidated (exec.rs:1325) then materialised as a Vec in restore_vm_core (:2691, ~34 GB for a 4 GiB range); `@mcopy`'s common `to >= from` branch overflows on iteration one (:2333); `@linearsearch` :3318, `@binarysearch` :3342, `@linkedsearch` :3364/:3372; `objects.rs:170` `array + count*4` on RAM-derived `count`, run on EVERY Glulx load via `ParseNames::detect`. `accel.rs`/`i7map.rs` already use wrapping/saturating — the model to copy.
9. **S — Glk version claims 0.7.6** (exec.rs:5831) but `0x00EC glk_image_draw_scaled_ext` and gestalt 24 are missing; **input structs ignore the −1 stack convention** (read_timeval :5085, read_glkdate :5097 — six date/time selectors fault); **suspended `glk_select` stores S1 before pushing the event words** (op_glk :3928, spec §2.18 order); **`glk_select_poll` returns Char/Line/Mouse/Hyperlink** (:4595) which §4 forbids, and never flushes; **`wintype_Blank` unimplemented** (glk.rs:36); **gestalt_CharOutput hardcodes ExactPrint** for every code point (:5843).
10. **S — `@setiosys` does not normalise.** exec.rs:1191: an unsupported mode is stored verbatim, so `@getiosys` reports 20; spec: "it will default to the null system (0)". glulxe also zeroes the rock for modes 0 and 2.
11. **G — accelerated functions never emit `accel_error`.** accel.rs:73 is a documented no-op; glulxe prints from 8 sites. accelfunctest's slow-vs-fast transcripts DIFFER on CP__Tab(new)/OC__Cl(new)/RV__Pr(new).
12. **G — fileref name simplification is non-standard.** glk.rs:1756 maps every non-`[A-Za-z0-9._-]` to `_` and keeps the extension; the Glk recommendation (cheapglk cgfref.c) is delete `/\<>:|?*"`, truncate at the first period, `"null"` if empty, then append `.glkdata`/`.glksave`/`.txt`. `externalfile.ulx` reports this as the ONLY divergence from glulxe. Means `.glkdata` files are not exchangeable with other interpreters.
13. **G — `@div`/`@mod` of 0x80000000 by −1** returns 0x80000000 (exec.rs:1755); glulxe fatals "Division overflow". **ROM writes** are a diagnostic + no-op (exec.rs:2178); glulxe fatals.
14. **Header/heap/memory are otherwise solid**: bounds via `.get()` + u64 arithmetic, stack is a fixed Vec with Fault on over/underflow, `reload_frame_meta` validates a restored frame, heap matches glulxe on every case tested. Missing at load only: stack-size 256-alignment, `start_func` range, `memsize >= ENDMEM` on a restored CMem/UMem (:2746/:2791), and checksum-at-load. **`Stks` IS byte-for-byte glulxe's portable form**, so gvm→glulxe already works; only the CMem reader blocks the reverse.

### Test coverage
`crates/gvm-cli/tests/glulxercise.rs` asserts **26 of 68 groups** and cannot see ANY of the five failures above. **No test drives any other corpus story** — accelfunctest, memheaptest, memcopytest, memstreamtest, resizememstreamtest, randomgen, datetimetest, selectvarianttest, unicasetest, externalfile, extbinaryfile, startsavetest, autosavetest and the rest are all unpinned; I ran them by hand. `crates/gvm-cli/tests/fixtures/README.md` is stale — it claims groups that are NOT in `IN_SCOPE` "all pass", says "double-precision opcodes are deferred (gestalt Double = 0)" (false: they are implemented and gestalt 13 = 1), and says `restore` is unwired (false: it passes).

### Parity confirmed against glulxe
extbinaryfile byte-identical; unicasetest folding lengths identical (real NFC/NFD tables, unicode_norm_tables.rs); datetimetest identical incl. local UTC offset; memstreamtest/memcopytest(overlap)/memheaptest/randomgen/resizememstreamtest/selectvarianttest all correct; searches honour KeyIndirect/ZeroKeyTerminates/ReturnIndex; string nodes 00–05 and 08–0B all present; 608 `-p lanthorn-gvm -p lanthorn-gvm-cli` tests pass.

### Performance vs glulxe -O2 (10 boots each, release)
BlueLacuna 2.37s vs 1.65s (**gvm 1.44x slower**); cragne 0.21s vs 0.17s (1.24x slower); Counterfeit Monkey 0.29s vs 1.19s (**gvm 4.1x faster**, from fingerprinted acceleration — CM ops 23.76M→2.67M, 8.90x — plus its own boot cache). Raw interpretation is ~1.2–1.5x behind glulxe; the wins come from acceleration, not from the dispatch loop.

Read-only throughout; `git status --porcelain` empty.
